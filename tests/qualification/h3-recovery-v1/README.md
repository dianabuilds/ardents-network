# Horizon 3 recovery development fixture v1

This human-authored Compose topology extends the Stage 3 process graph only
with a networkless, read-only independent recovery verifier. Generated plans,
credentials, workload seeds, run state, and retained evidence remain outside
the repository.
