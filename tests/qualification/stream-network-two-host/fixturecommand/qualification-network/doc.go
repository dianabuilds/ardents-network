//go:build linux

// Command qualification-network creates one bounded private fixture bundle for
// the installed two-host stream qualification. Product commands still accept,
// sign and initialize every durable runtime root from these inputs.
package main
