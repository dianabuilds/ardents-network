//go:build ignore

// Command wrapper is the disposable multi-agent real-network experiment
// coordinator. It turns schema-checked refresh/noop decisions into commands
// with fixed persona-owned paths and immutable evidence. It is not a
// maintained product command.
package main
