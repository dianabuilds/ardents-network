# S3.6.5 local qualification results

Status: **PASS; owner acceptance pending.** The qualification ran only on the
local Docker host. No VPS was used.

## Bounded verdict

The repaired harness kept one stable State root, local-role root, and source
plan per persona. In the accepted sequential run, the honest consumer made 11
successful refreshes without accumulating local-role conflicts, the battery
consumer accepted once and then deliberately idled, and the probe observed the
expected `invalid-state` outcome once before idling through durable backoff.

This result qualifies the S3.6.5 harness. It does not qualify four concurrent
personas, a public network, independent operators, or a direct-source-cap DoS
claim.

## Qualification run

- Run ID: `20260904T050603Z-1f8ccd757797`
- Execution order: sub-run A completed before sub-run B started.
- Accepted generation:
  `eaa47fa4aa2c425dd26f015145b1222f25ce875a505da08b14839c8a5cc1eee6`
- Manifest SHA-256:
  `3897046731b936f346958678f4caa1f711df56a0b88f8957351a4590a5554a9a`
- Wrapper SHA-256:
  `718a06c8a64c54bc43fe6d9604f5b2f2796ae1ffa44503d44990e55e99861b48`
- Product artifact SHA-256:
  `36a1a960c3c95090daed09715f4f6cd762ff92aa79972b91c34912a166c82123`
- SHA-256 of the ordered 19-event hash list:
  `b563a6ec557a135733707eb0aa4f337e010eab7d610727fb92347f870ef3328f`
- Evidence root:
  `%LOCALAPPDATA%\Temp\ardents-multi-agent-final-83280a1d213c4965a846d96081c5b6a3`

| Persona | Evidence | Span | Result |
|---------|----------|------|--------|
| `honest_user` | 11 events | 309 s | 11 `accept`; exact `[valid, valid, not-attempted, not-attempted]` signature |
| `battery_saver` | 3 events | 308 s | 1 `accept`, then 2 `noop`; exact honest signature on the refresh |
| `probe_consumer` | 5 events | 181 s | 1 expected `reject`, then 4 `noop`; exact `[valid, invalid-state, not-attempted, not-attempted]` signature |

`wrapper verify` returned exit 0 with 19 events, one shared generation, no
`infra_error`, and no `harness_abort`.

## Preserved falsification attempt

Run `20260904T032656Z-96765fed4896` deliberately remains in the earlier
`ardents-multi-agent-qual-8471507027ed4563b82a8146ca1ee72e` external evidence
root. Its third probe refresh failed with the raw error
`retry is in durable backoff`. That attempt falsified the original repeated-
refresh probe policy: after the expected rejection, the product correctly
enters durable backoff. The older wrapper binary labelled that retained event
`cycle_deadline`; the current classifier checks the specific backoff text first
and its exact-string regression test passes.

The accepted run therefore uses one evidence-producing probe refresh followed
by bounded noops. A later success never erases the failed attempt.

## Acceptance criteria

| AC | Status | Evidence |
|----|--------|----------|
| AC1 build | PASS | `cmd/wrapper/test.ps1`; named-source build exit 0 |
| AC2 vet | PASS | `cmd/wrapper/test.ps1`; named-source vet exit 0 |
| AC3 honest | PASS | 11 accepts over 308 seconds |
| AC4 battery | PASS | first accept plus two noops over 308 seconds |
| AC5 probe | PASS | one expected reject plus four noops over 181 seconds |
| AC6 preflight | PASS | unit tests reject unknown fields, injected paths, trailing input, and plan drift |
| AC7 immutable evidence | PASS | unit tests cover strict names, gaps, no-overwrite publication, and exclusive writers |
| AC8 lifecycle | PASS | no record-limit, producer-limit, conflict, or legacy-bound abort in the qualification run |
| AC9 scope | PASS | question registration, product diagnostics, and experiment harness are prepared as three separate commits |
| AC10 single writer | PASS | unit test plus local process contention: one lease owner and three competing wrappers rejected with `persona already has an active writer` |

The AC10 contention check used separate throwaway run
`20260904T051549Z-3d32cfc78e21` with the final wrapper binary. Exactly one
process acquired the lease and completed its refresh; all three simultaneous
competitors were rejected before Docker execution.

## Commands exercised

```powershell
go test ./internal/network/duty -count=1
& experiments/multi-agent-real-2026-09-04/cmd/wrapper/test.ps1
& 'C:\Program Files\Git\usr\bin\bash.exe' -lc `
  './experiments/multi-agent-real-2026-09-04/cmd/wrapper/test.sh'
docker compose --profile build up --detach builder prebake
docker compose up --detach --wait
& $wrapper verify $manifest
docker compose down
```

Both wrapper test entry points ran build, vet, unit tests, and `self-test`
successfully. Git Bash was selected explicitly so Windows did not route the
script through the unavailable WSL service.

## Disposition

Retain the wrapper as disposable research infrastructure and keep all runtime
artifacts outside Git. S3.6.5.1 (four personas) and any cap-exhaustion security
experiment remain deferred until separately contracted.
