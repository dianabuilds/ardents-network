// Command ardents-release-custody initializes and inspects the Product Owner's
// local release-seed envelope. It writes no secret into the repository and
// returns only a non-secret public receipt.
package main
