# Package map

This is the factual naming and dependency registry for maintained Go code. It
lists packages that exist, not future intentions. A new entry is an architecture
change: name the responsibility first, add the real Implementation and tests,
then update this table in the same change. The normative growth and dependency
rules are in [repository-layout.md](repository-layout.md).

R-091 retired the closed laboratory namespace. Its provenance is retained in
Git history; no current package may import or start a lab Module.

| Directory | Go declaration | Responsibility | May import |
|---|---|---|---|
| `cmd/ardents` | `package main` | Parse its own bounded offline or Direct-Origin Source inputs, call Network State, run one role-local Endpoint process through `endpoint run`, retain the selected named-alpha Browser runtime through `endpoint alpha-browser` from existing State/Entry/corpus owners without accepting a route, and, when the same bounded Source plan is declared, take that root's initial/automatic State refresh rather than let a competing process hold the lease; run the selected per-user Portable lifecycle through `endpoint portable`, explicitly preflight one independently pinned closed-alpha bundle through `endpoint enrollment-check`, compose either that bundle or the selected Ubuntu Installed package-enrollment input plus Release Decision before Portable readiness through `endpoint enroll` / `endpoint enroll-installed`, render (without installing, enabling, or starting) the matching exact Ubuntu `systemd --user` unit through `endpoint user-unit` / `endpoint installed-user-unit`, run the no-network candidate-side H4-1B successor-record check through `endpoint replacement-self-test`, report only durable H4-1B classification through `endpoint replacement-recovery`, execute one explicit local `endpoint replace` bundle against the exact `ardents-endpoint.service` user unit, and let only the journal-bound retained recovery program execute `endpoint rollback` with a fresh local Release bundle; it can also import one State-referenced signed Entry Invite through `entry import` or execute selected naming operations through `name`, rendering bounded receipts. | `internal/browserentry`, `internal/endpoint`, `internal/endpoint/enrollment`, `internal/endpoint/portable`, `internal/endpoint/replacement`, `internal/entry`, `internal/naming`, `internal/naming/alpha`, `internal/naming/namespace` only for its opaque Resolution views in behavior fixtures, `internal/naming/namespace/admission`, `internal/naming/namespace/authority`, `internal/naming/namespace/epoch`, `internal/naming/namespace/record`, `internal/naming/resolution`, `internal/network/duty`, `internal/network/state`, `internal/network/source`, `internal/release`, `internal/route`, standard library |
| `cmd/ardents-browser-entry` | `package main` | Run the Firefox native-messaging stdio host. A native manifest invokes it without arguments, so it reads one fixed per-user Endpoint Browser Entry state path; a qualification may explicitly supply one absolute state path through `native-host --state`. It returns only a live-probed loopback alpha proxy port, or that same port plus the separate local proxy password for the bounded revalidated authentication operation, and rejects every other native operation. Its separate `install` route first verifies an exact enrollment-v4 Endpoint artifact, itself as the separately manifest-pinned native-host companion, and the matching fixed-ID XPI with Mozilla COSE signing metadata, then installs only its fixed per-user native manifest; Firefox remains the cryptographic signature verifier during the participant's explicit XPI installation. `remove` withdraws only that registration. It never installs an extension, launches an Endpoint/browser, resolves a name, selects a Target, proxies traffic, or writes State. | `internal/browserentry`, `internal/browserentry/installer`, `internal/endpoint/enrollment`, standard library |
| `cmd/ardents-custody` | `package main` | Run the separate custody-process adapter. `inspect-envelope` renders only public canonical envelope-header facts; `verify-record` consumes a password only through an interactive no-echo terminal and renders bounded non-secret verification facts; `export-recovery-bundle` and `restore-recovery-bundle` expose respectively a distinctly passworded test-restored Bundle and an authority-locked quarantine restore; `purge-record` deletes one exact verified encrypted record only after terminal confirmation while retaining its Authority floor. It never accepts Authority material or a password through arguments, environment, configuration, or shared application stdin; it does not activate a recovered Authority. | `internal/custody`, `golang.org/x/term`, standard library |
| `cmd/ardents-release-custody` | `package main` | Run the Product Owner's separate local release-custody adapter. `initialize` receives only an existing owner-only root path, reads a new passphrase and confirmation, and publishes the encrypted record; `inspect` receives that same root plus one local passphrase and returns its public fixed-role receipt. `assemble` accepts only one fixed public request, exact Endpoint/control artifact paths, and a previously absent external output directory. `assemble-successor` additionally admits only the pinned RC1 static predecessor and RC2 inputs, then renders the bounded public generation-2 preflight receipt. It never accepts a secret through arguments, environment, configuration, or shared stdin; it does not upload, execute, install, start an Endpoint, or configure a VPS. | `internal/release/custody`, `golang.org/x/term`, standard library |
| `cmd/ardents-state-custody` | `package main` | Run the Product Owner's separate functional-alpha State-custody adapter. `initialize-alpha-genesis` receives only one existing owner-only absolute root, reads a new passphrase and confirmation, then creates the fixed encrypted 1-of-1 Epoch seed plus its public empty-topology State request fragment and renders a non-secret receipt. It accepts no key, Network identifier, signer, profile, threshold, Epoch, Node, time, URL, upload, or publication destination from the caller. | `internal/network/state`, `golang.org/x/term`, standard library |
| `cmd/ardents-control` | `package main` | Run the separate H4-6A alpha-control reader. Its participant `inspect-bundle` route verifies the exact alpha-enrollment input/pin and claims one marked inspection root with fixed catalog, Release, and Network children; its lower-level `inspect-alpha-corpus` route diagnoses explicit ACA2/corpus files under supplied roots. Its `accept-alpha-corpus` route first verifies an exact enrollment-v3-or-later bundle, its `corpus.pub` companion, and that the running platform-specific control command is the manifest-pinned companion, then verifies all ACA1 control evidence and accepts explicit ACA2/corpus bytes into only an explicitly named Endpoint-local corpus floor. It never downloads, starts an Endpoint, or supplies a catalog/component root or byte to active Release, Network State, Namespace, Update, Route, or Endpoint acceptance. | `internal/alphacontrol`, `internal/alphacontrol/inspection`, `internal/endpoint/enrollment`, `internal/naming/alpha`, standard library |
| `cmd/ardents-node` | `package main` | Parse its own bounded Source or Node configuration and run one private distributor or separately keyed Node process. A Source may explicitly serve the exact maintained native Interactive Route State profile; a Node supplies one or more explicit finite local Rendezvous, Initiator, Introduction, or Responder reservations, while State selects the one runnable duty. Rendezvous alone may use an optional same-port literal-loopback operational bind behind a host Carrier relay; State still owns the advertised candidate and every authenticated duty fact. Its Linux-only `contributor apply` route adapts the exact independently pinned H4-5 Rendezvous dedicated-host lifecycle to systemd; it exposes no other duty or arbitrary service operation. | `internal/contributor`, `internal/network/state`, `internal/network/source`, `internal/node`, `internal/resource` (diagnostic behavior test only), `internal/route`, standard library |
| `tests/e2e/endpoint` | `package endpoint` with external `endpoint_test` cases | E2E-only public-command evidence for the closed-alpha Enrollment Pin, H4-6A reader, alpha-corpus intake, and H4-1 lifecycle. It builds and executes the exact bundle artifact, accepts only its pinned local inventory, proves changed manifests fail before parsing, synthesizes ephemeral signed TUF metadata for that exact artifact to exercise Release Decision, Portable readiness, floor persistence, and clean stop. Linux cases additionally prove H4-1B fixed-unit local replacement/recovery and H4-1D direct-package install, distinct-version upgrade with fresh Release authorization, remove, and purge while protected state remains external. It neither signs/releases a bundle nor proves participant provenance or an enrolled network runtime. | `internal/alphacontrol`, `internal/alphacontrol/inspection`, `internal/naming/alpha`, `internal/release`, `github.com/sigstore/sigstore/pkg/signature`, `github.com/theupdateframework/go-tuf/v2/metadata`, standard library |
| `tests/e2e/service/fixturecommand/publish-app` | `package main` | E2E-only separately granted publication-operator fixture. It uses only the bounded local Service Administration socket and is built solely by the Endpoint recovery process test. | standard library |
| `tests/e2e/service/fixturecommand/stream-app` | `package main` | E2E-only external opaque-stream Application fixture. It owns its supplied local byte/result streams and paired direct workload only as process-test evidence; it is built solely by the Endpoint recovery process test. | `internal/endpoint`, standard library |
| `tests/e2e/service/fixturecommand/reference-c2` | `package main` | E2E-only multi-process fixture for one fixed C-2 scenario: Publisher and its token-bound loopback Application; User and independent alpha-resolution Observer Endpoints; separate alpha OHTTP Gateway/Relay, Destination Resolution Gateway, Introduction, Initiator, Rendezvous, and Responder. It builds one exact publication and fixture-only signed alpha corpus, writes distinct User/Observer durable floors, and permits only the User to continue from its accepted binding into C-2. H4-3A carries three static resources; H4-3B carries an ordinary HTTP/1.1 form POST/body/header/cookie, redirect, cookie follow-up, and chunked response through one selected Service. A11 replaces only the fixture Rendezvous with the exact product command behind a bounded raw-TCP Carrier relay role; the relay owns no TLS or State facts and emits exact ready/reset/final counters. Its bounded failure cells explicitly unpublish the exact Target/generation after drain, reset the local Publisher Application after a partial response, hard-stop the exact Publisher Endpoint or product Rendezvous PID, or reset the oldest active Carrier relay bridge while the second bridge and listener remain live; they require exact clean/abrupt terminal classes and no name/Internet fallback. The ordinary fixture client sends a visible exact `.ard` name only to Endpoint's alpha proxy, permits one physical proxy TCP dial, and rejects every reconnect. The Windows Browser Entry qualification starts from `reference.ard`; Endpoint resolves it from the accepted floor and opens its exact Service before Firefox performs the dynamic flow. It neither configures a participant browser nor claims generic web-app compatibility. The fixture uses durable State roots, a real canonical Entry Invite, State-authority-signed spent Transit Grants, and explicit offline/application-refusal paths; it is neither participant configuration nor a route or credential runtime. | `internal/application/broker`, `internal/endpoint`, `internal/endpoint/reference`, `internal/entry`, `internal/naming/alpha`, `internal/naming/alpha/private`, `internal/network/duty`, `internal/network/source`, `internal/network/state`, `internal/node`, `internal/route`, `internal/service/publication`, `internal/service/reachability`, standard library |
| `tests/e2e/node/fixturecommand/netem-relay` | `package main` | H4-2-qualification-only static TCP relay. It accepts one declared Linux netem mode (`delay`, 100% `drop`, or fixed delay/loss/reorder), applies it only to its own Docker `eth0` through a supplied `tc`, and forwards at most 1 MiB in each direction to one supplied upstream. It receives no Node State, credentials, Route, or Service input and is neither a maintained Node nor a Carrier implementation. | standard library |
| `internal/entry` | `package entry` | Own strict native-Profile, State-referenced Entry Invite v1 canonical issuance, decoding, and validation, including the Initiator-side current-fact verification and bounded durable EntryBinding replay ledger; a two-slot replacement/replay set; a bounded durable adjacent-contact lifecycle with restart settlement; and State-derived candidate lookup. It never selects a Route, implements a carrier, retains a signer, or creates a User identity. | standard library; `golang.org/x/sys/windows` on Windows |
| `internal/alphacontrol` | `package alphacontrol` | Own the closed H4-6A disclosure-catalog and component-statement codecs, exact fixed component binding, separately enrollment-pinned Ed25519 disclosure/component-root verification, and reader-owned catalog/component floors. It reports reader-only inspection outcomes and never authorizes Release, Network State, Namespace, Update, Route, or Endpoint behavior. | `golang.org/x/sys/windows` on Windows, standard library |
| `internal/browserentry` | `package browserentry` | Own the bounded per-user Firefox native-host contract: current AlphaProxy loopback-port state, separate random liveness-probe and proxy-authentication values, atomic stale-state removal, and framed port/authentication answers only after the exact local proxy answers its fresh probe. It stores no Name, Target, Route, Service credential, or application data; it never resolves, dials a Service, installs an extension, or routes browser traffic. | standard library |
| `internal/browserentry/installer` | `package installer` | Own the per-user native-manifest lifecycle for the one fixed Firefox Browser Entry ID: strict local host/XPI shape checks including the current Mozilla COSE signing surface, owned-only atomic manifest creation/replacement/removal, and platform-specific current-user Firefox registration. It never installs the XPI, starts/configures Firefox, selects Endpoint state, cryptographically verifies Mozilla's signature, or verifies a release; Firefox and its caller own those respective checks. | `internal/browserentry`, `golang.org/x/sys/windows/registry` on Windows, standard library |
| `internal/contributor` | `package contributor` | Own one exact H4-5 dedicated-host Rendezvous profile from independent manifest-pin verification before parsing through its closed binary/config inventory, fixed host paths and hardened systemd unit, generation-bound installation/update record, bounded READY diagnostics, supervisor-driven restart/drain/withdrawal, and exact confirmed removal. It cannot enroll another duty, select Network State or peers, admit a public operator, preserve a removed identity, or claim capacity, availability, or independence. | standard library |
| `internal/alphacontrol/inspection` | `package inspection` | Compose one enrollment-pinned H4-6A bundle with separate catalog, Release, and Network State inspection roots. It verifies typed Release, Network, and Compatibility statements against their independently owned verifier results and emits only a non-authorizing report; its ACA2 verifier can additionally require all accepted ACA1 components, then check one exact Alpha Name Corpus under the independently enrollment-pinned v2 corpus root. It never starts an Endpoint or writes a corpus floor. | `internal/alphacontrol`, `internal/endpoint/enrollment`, `internal/naming/alpha`, `internal/network/state`, `internal/release`, standard library |
| `internal/network/duty` | `package duty` | Own the bounded, owner-only, durable Endpoint-local Role Domain duty generations, watermark, expiry, conflict truth, and Node-local one-use Transit Grant spend ledger used by maintained role producers. The grant ledger records only Node ID, opaque Grant ID, and expiry; it owns no Route, Target, Service, or client material. | `golang.org/x/sys/windows` on Windows, standard library |
  | `internal/naming` | `package naming` | Own Stage 6 canonical Service Name parsing, normalization, and deterministic encoding checks as production-owned input gates. | standard library |
  | `internal/naming/alpha` | `package alpha` | Own the explicitly non-Namespace Alpha Service Link grammar and its finite signed Alpha Name Corpus: canonical alpha-only binding encoding, authority verification, expiry, authenticated total withdrawal, classified no-fallback outcomes, and one Endpoint-local owner-only durable signed-corpus serial/digest floor. Its lease precedes interrupted-write recovery and it never presents a result as the current canonical Namespace or as public registration. | `internal/naming`, `golang.org/x/sys/windows` on Windows, standard library |
  | `internal/naming/alpha/private` | `package private` | Own one alpha-only fixed-size OHTTP Client/Relay/Gateway exchange over an already verified finite Alpha Name Corpus. The Relay forwards only opaque bytes to its fixed Gateway; the Gateway receives the requested Alpha Service Link only after OHTTP decapsulation and returns the complete signed corpus. Neither retains per-request observations. The Client verifies corpus authority/cohort/network and decision-time validity before applying its supplied session or durable Endpoint-local floor; it never falls back to Target Links or canonical Namespace resolution. | `internal/naming`, `internal/naming/alpha`, reviewed OHTTP/CIRCL closure, standard library |
  | `internal/naming/namespace` | `package namespace` | Compose the authenticated Namespace modules and own only the opaque private-Resolution Gateway/verifier views. Concrete Namespace types and transitions belong to their cohesive submodules; root external-package tests exercise those owners directly. | `internal/naming/namespace/admission`, `internal/naming/namespace/authority`, `internal/naming/namespace/claim`, `internal/naming/namespace/epoch`, `internal/naming/namespace/record`, `internal/naming/namespace/recovery`, standard library |
| `internal/naming/namespace/admission` | `package admission` | Own one boot-scoped finite anonymous-work gate: authenticated challenges, bounded work, replay spending, expiry, capacity, and in-flight refusal. | standard library |
| `internal/naming/namespace/recovery` | `package recovery` | Own Recovery Policy validation, quorum-proof verification, and sealed Recovery Authorization facts. | `internal/naming`, standard library |
| `internal/naming/namespace/record` | `package record` | Own canonical Name Records, signature containers, lifecycle/lease transitions, parent lineage, and destination bindings. | `internal/naming`, `internal/naming/namespace/recovery`, standard library |
| `internal/naming/namespace/claim` | `package claim` | Own admitted root-claim commitment/reveal inputs, authenticated close verification, and one-use materialization of a winning root claim. | `internal/naming`, `internal/naming/namespace/admission`, `internal/naming/namespace/record`, standard library |
| `internal/naming/namespace/epoch` | `package epoch` | Own the compatible durable Namespace root, exact-successor pending journal and cursor, current/pending Epoch materialization, attestation, compact proof verification, and opaque current Name-Authority reconciliation witnesses. | `internal/naming`, `internal/naming/namespace/claim`, `internal/naming/namespace/record`, standard library |
| `internal/naming/namespace/authority` | `package authority` | Own canonical private control submissions and authorized transitions over admitted proofs, claim closes, record lifecycle, and recovery. It writes only already-authorized successors through the Epoch pending port; it owns no durable materialization queue. | `internal/naming`, `internal/naming/namespace/admission`, `internal/naming/namespace/claim`, `internal/naming/namespace/epoch`, `internal/naming/namespace/record`, `internal/naming/namespace/recovery`, standard library |
| `internal/naming/resolution` | `package resolution` | Own authenticated role selection, admitted immutable Binding exchange through Namespace and State Resolution views, fixed-size OHTTP Client/Relay/Gateway resolution and control exchange, finite replay rejection, observer-safe counters, and fail-closed local behavior without a plaintext fallback. It imports the root Namespace only for its owned Gateway/verifier views; concrete admission, control, claim-order, materialization, and Binding types come from their cohesive Namespace submodules. | `internal/naming/namespace`, `internal/naming/namespace/admission`, `internal/naming/namespace/authority`, `internal/naming/namespace/claim`, `internal/naming/namespace/epoch`, `internal/naming/namespace/record`, `internal/naming`, `internal/network/state`, reviewed OHTTP/CIRCL closure, standard library |
| `internal/network/source` | `package source` | Own the finite Direct-Origin Source plan, credential binding, private TLS transport, material selector, ordering, and exposure identity. | standard library |
| `internal/network/state` | `package state` | Own authenticated Network State acceptance, Epoch/View verification, commitments, deterministic role-domain assignment, materializations, current/pending decisions, finite acquisition, clock confidence, Direct Source duty retention, its exclusive durable root, bounded immutable generations, distribution journal, canonical byte cursor, atomic publication, and narrow private Resolution and Node-duty views. For an interactive Epoch, its private Resolution view projects exactly one State-assigned Destination Resolution Gateway and one `transit-issuance` issuer identity/family with their opaque signed profile bytes; State neither parses these owner-owned grammars nor ranks alternatives. Its ADR-0053 `InitializeAlphaGenesis` interface alone creates the separate encrypted 1-of-1 functional-alpha Epoch seed and fixed verifier-accepted empty genesis; it exposes no generic signer, successor, Node key, or caller-selected/non-empty topology. Product tests own independent canonical vector builders. | `internal/network/duty`, `internal/network/source`, `internal/resource`, `golang.org/x/crypto/argon2`, `golang.org/x/sys/windows`, standard library |
| `tests/epochfixture/assignment` | `package assignment` | Independently construct deterministic role-domain assignments for black-box Network State fixtures; never serve maintained product code. | standard library |
| `tests/epochfixture/merkle` | `package merkle` | Independently construct canonical Epoch commitment roots and proofs for black-box Network State fixtures; never serve maintained product code. | standard library |
| `internal/route` | `package route` | Own native v1 endpoint selection and volatile Route Attachment lifecycle behind `Open`, `Attach`, and `Close`: caller-owned resource reservation, State-pinned User-to-Initiator TLS, opaque-capability Endpoint-to-Introduction/Responder TLS admission, State-authority-signed opaque Transit Grant v1 issue/verification, live Introduction-slot registration, explicit C-2 outcomes, and exact sealed-control forwarding, Endpoint-selected RelaySetup/RelayReady next-leg authorization, and separate one-use ResolutionRelay and CredentialRelay setup/ready plus bounded opaque OHTTP request/response records; exact Node-to-Node TCP/TLS or QUIC-v1 Carrier open with common TLS/LegBinding authentication; concurrent-selection exclusion; bounded cancellation; and closed EntryBinding, EndpointTransitBinding, IntroductionSlotRegistration, IntroductionSlotReady, IntroductionDeliveryResult, and SealedIntroduction codecs. It exposes only finite attachment and opaque operation carriers, never a complete Route plan or generic proxy. It has no H3 reader, peer runtime, Node profile, or peer announcement. | `internal/entry`, `internal/network/state`, `github.com/quic-go/quic-go`, standard library |
| `internal/route/credential` | `package credential` | Own the versioned membership-level Transit Grant issuance operation: signed State-selected issuer OHTTP profile, fixed target-free request/response grammars, a one-use client that verifies the exact returned Grant, a project-operated issuer handler that rechecks the typed current State duty, and an exact mutual-TLS HTTPS forwarder. Its Endpoint exchange is already an admitted opaque Credential Relay; it never sees or selects a Name, Target, Publisher, Entry Invite, Route peer, or fallback. | `internal/route`, reviewed OHTTP/CIRCL closure, standard library |
| `internal/service/publication` | `package publication` | Own one exclusive C1 Service Instance publication root, monotonic generation floor, crash-atomic immutable public record, volatile Instance signer, acquisition references, withdraw/supersede drain, private-material erasure, and the Service-only Introduction instruction that binds one decrypted C-2 handoff to the exact current publication. A restart retains only public proof and floor, never a live private Instance. | `golang.org/x/sys/windows` on Windows, standard library |
| `internal/service/reachability` | `package reachability` | Own the closed Instance-signed Service Reachability Descriptor v1 fixed-Grant and v2 membership-Grant declarations; Gateway-local durable per-Target generation/conflict state; the signed, bounded GatewayProfile codec; and the fixed-size OHTTP Gateway/Client adapter for one opaque Target lookup. It verifies the Target-derived Authority, complete Publication, Instance signature, slot expiry, and bounded fields before Endpoint composition; Store atomically retains one accepted descriptor or a persistent same-generation conflict and refuses downgrade/overlapping higher generations across restart. Its one-purpose forwarder accepts only a bounded OHTTP envelope to one State-selected HTTPS Gateway and pins that Gateway's Ed25519 certificate key; it does not select State peers, discover a Gateway, acquire Entry, issue a Transit Grant, or create a Service Connection. | `internal/service/publication`, reviewed OHTTP/CIRCL closure, `golang.org/x/sys/windows` on Windows, standard library |
| `internal/service/connection` | `package connection` | Own immutable Service Connection facts — pinned Service Name provenance and finite recovery constraints — the closed unannounced native Service Connection v1 record grammar, and the logical stream lifecycle: ConnectionContext, exact Instance challenge/proof, attachment continuity, ordered/replayed bounded data, acknowledgement, replacement, explicit per-direction terminal outcome, and cleanup. It supports either exact declared workloads or orderly half-close before the caller's directional byte bounds. It accepts only opaque already-authenticated Attachments and an Application byte stream; it has no legacy reader, profile negotiation, direct fallback, publication private key, Application IPC authorization, or Route selection authority. | standard library |
| `internal/service/targetlink` | `package targetlink` | Own the closed canonical `ardents-target:v1:` text form: fixed Target algorithm identifier, exact network binding, and exact opaque Target bytes. It neither resolves reachability nor authenticates a remote Service Instance. | standard library |
| `internal/endpoint` | `package endpoint` | Owns role-local Endpoint composition, bounded plan loading, scoped Broker/Application IPC, exact Publication/Target/Connection composition, Route-Attachment lifecycle, and one classified terminal result. It binds a Target Link or ACA2-verified Alpha Name Corpus binding to one authenticated Target before creating either H4-3A's closed Reference Site or H4-3B's payload-neutral HTTP/1.1 presentation. Its optional Alpha Browser Resolution owns the direct `name.ard` demand path: a loopback proxy validates the suffix, resolves the corresponding alpha link only from an Endpoint-local accepted durable floor, and asks one caller-owned C-2 composition to open that exact binding; concurrent requests share that opening. Its Alpha Browser Runtime composes that demand only from an opened State Resolution view and imported Entry owner: State projects the Gateway profile, C-2 candidates, and—only for Descriptor v2—the sole transit-issuance issuer profile. Endpoint creates an ephemeral key/attachment, carries one target-free OHTTP request through Credential Relay, verifies and temporarily enrolls the exact resulting Introduction Grant, then erases that enrollment after its sole opening; it accepts no caller Target, profile, peer, issuer URL, route, or fallback. Descriptor v1 instead uses its embedded fixed Grant and pre-enrolled matching key. It is not an OS resolver, public DNS, arbitrary proxy, canonical Namespace, or public HTTPS claim. With an Endpoint-owned Browser Entry state path it publishes only the current proxy port and fresh probe capability, then clears both state and proxy at withdrawal. Its Firefox adapter launches an explicitly selected external browser only for an already-created scoped `127.0.0.1` Reference URL or provisional alpha `.ard` address; it never accepts the deprecated loopback hostname form, chooses an arbitrary URL, owns a browser profile, or changes browser, proxy, DNS, VPN, or trust settings. The remaining Endpoint paths perform one bounded private Target lookup and C-2 route composition without acting as a route planner or Service lookup. | `internal/alphacontrol`, `internal/alphacontrol/inspection`, `internal/application/broker`, `internal/browserentry`, `internal/endpoint/reference`, `internal/route`, `internal/route/credential`, `internal/service/connection`, `internal/service/publication`, `internal/service/reachability`, `internal/service/targetlink`, `internal/entry`, `internal/naming/alpha`, `internal/naming/alpha/private`, `internal/network/state`, `internal/naming/namespace/record` and `internal/node` in behavior tests, `golang.org/x/sys/windows` on Windows, standard library |
| `internal/endpoint/reference` | `package reference` | Own H4-3A's fresh loopback-only, opaque-path browser presentation origin for an already-authenticated Target and bounded static Reference Site resources, plus H4-3B's connection-scoped transparent HTTP/1.1 presentation for one selected Service Connection. The transparent presentation preserves HTTP request/response semantics and streaming without a content profile, serializes requests on that connection, treats its selected Publisher's authenticated terminal as a local presentation close, and explicitly rejects upgrades. Its separate alpha proxy accepts proxy-form plain HTTP only for exact `.ard` names and dials only the matching local presentation; failure of one registered Target cannot select another registered route. An Endpoint may install one fixed demand opener: it can open a previously unregistered exact alpha name only by registering that name before the request continues, and concurrent requests wait for the same opening. The proxy itself never parses an alpha link, resolves a Target, dials a Service, chooses an upstream, or accepts an ordinary hostname; it rejects HTTPS CONNECT, non-HTTP schemes, and all other destination choice. When Endpoint configured the native Browser Entry, it additionally proves its fresh random liveness capability on one loopback-only fixed probe path; that path exposes neither a Service nor proxying. Closing a route withdraws only that name. | `internal/browserentry`, standard library |
| `internal/endpoint/enrollment` | `package enrollment` | Own the independently pinned closed-alpha first-artifact verification: exact manifest pin before parsing, bounded static regular-file inventory, canonical enrollment descriptor, exact bundled-artifact or one explicit package-owned artifact match, Release Decision byte-input construction, the non-secret local enrollment input parser, and the optional enrollment-v2 `corpus.pub`, enrollment-v3 manifest-pinned control companion, and enrollment-v4 Browser Entry host/XPI companion projections. It can prove a current companion process against already verified manifest bytes; it never downloads, executes, installs, updates, or grants authority to an artifact. | `internal/browserentry`, `internal/release`, standard library |
| `internal/endpoint/portable` | `package portable` | Own the H4-1A per-user Portable Endpoint profile: final platform-resolved configuration/state/cache/runtime roots, owner-only path policy, one held state-owner lock, guarded stale filesystem-socket recovery, a bounded generic local attachment probe, visible lifecycle outcomes, and terminal cleanup. It creates neither an Application capability nor an Endpoint network route, and makes no same-user isolation claim. | `golang.org/x/sys/windows` on Windows, standard library |
| `internal/endpoint/replacement` | `package replacement` | Own the H4-1B Ubuntu-only Endpoint-program binding between one opaque Release authorization and the exact executable permitted as a committed successor: owner-private current/prepared/journal records, held state lease, owner-only executable predecessor retention, bounded direct-file digest checks, same-directory atomic activation, interruption classification, and candidate-side no-network matching result. After an activated candidate self-test fails, it admits rollback only from that exact journal-bound retained predecessor copy and only with a fresh opaque Release authorization for the retained bytes; it owns no downloader, service-manager identifier, Endpoint route, Vault, Release floor store, or inferred local-byte rollback authorization. | `internal/release`, `golang.org/x/sys/unix` on Linux, standard library |
| `internal/node` | `package node` | Own one bounded local Node identity credential, assignment admission, the retained private role-probe listener, the joined installed-Contributor clock observation, and the H4-2 native duties: State-supplied peer/Carrier-profile/Entry-verification facts and current State-authority verification keys; Rendezvous mutually authenticated TCP/TLS-or-QUIC Carrier legs, exact LegBinding admission, separate handshake/waiting/pair reservations and pair pumping; Initiator's owned durable Entry admission, Endpoint-selected C-2 RelaySetup validation and one exact State-pinned Rendezvous Carrier dial, plus separate ResolutionRelay and CredentialRelay operations that forward one opaque OHTTP envelope to exactly one certificate-pinned State Gateway or transit issuer and then close; Introduction opaque transit admission, finite live slots, one-use sealed-byte forwarding, and explicit C-2 outcomes; and Responder Publisher-side C-2 admission, one exact State-pinned Rendezvous Carrier leg, and bounded relay pumping. Rendezvous may replace only its local listen socket with a same-port literal-loopback bind; the authenticated State candidate and duty report remain unchanged. Lifecycle evidence includes the selected Carrier profile. It neither decrypts a SealedIntroduction or OHTTP envelope, discovers peers, selects a Carrier fallback, capacity, or a complete Route. | `internal/entry`, `internal/network/duty`, `internal/resource`, `internal/route`, `internal/route/credential`, `internal/service/reachability`, standard library |
| `internal/resource` | `package resource` | Own Linux cgroup-v2/rlimit, Go-runtime, process-socket, and bounded managed-root storage measurement; the retained fixed H3 placements; the purpose-bound H4-5 Rendezvous dedicated-host Functional Alpha placement; hysteresis; and the finite NORMAL/PROTECT/DRAIN pressure decision behind `Check` and `Observe`. Unsupported platforms explicitly refuse so consumers own readiness, admission, drain, and shutdown reactions without inventing resource policy. | standard library |
| `internal/architecture` | `package architecture` | Test repository structure, names, dependencies, formatting, and quality wiring. | standard library |
| `internal/application/broker` | `package broker` | Own finite volatile Local Grant admission for explicit `connection` and `administration` surfaces: exact Principal-to-Grant matching, opaque one-use capabilities, expiry, receipt commitments, immediate grant revocation, and terminal close. A finite drain may affect only a Grant explicitly configured to permit it. Its only isolation result is `generic/unqualified`; it has no sandbox, process-host, or qualified isolation authority. | standard library |
| `internal/custody` | `package custody` | Own canonical `ardents-authority-envelope-v1` admission, fixed Argon2id/AES-GCM protection, exclusive encrypted Vault records, bounded per-operation secret use, public-only envelope inspection, distinct-password Recovery Bundle export with isolated test restore, confirmed replacement that preserves the old encrypted Bundle on ordinary publication/test failure, encrypted authority-locked export-only restore into a previously empty Vault quarantine root, and an exact non-decreasing local Authority floor. Record/floor writes flush ciphertext, use same-directory platform durable replacement, reopen before acknowledgement, and advance a floor only after encrypted publication; active verification requires exact floor agreement. A locked Name Bundle may activate only from a fresh opaque witness of one current active Namespace Authority strictly above its stored generation/revision; activation durably creates a higher-watermark active successor and floor before a signature can occur. An active Name record can sign one exact sealed transition or one Namespace-derived transition/successor-Record pair only when its Authority key, generation, and revision match the active record, without exposing its root. Runtime Instance Keys and Local Grants remain outside custody. | `internal/naming/namespace/admission`, `internal/naming/namespace/authority`, `internal/naming/namespace/epoch`, `internal/naming/namespace/record`, `golang.org/x/crypto/argon2`, `golang.org/x/sys/windows` on Windows, standard library |
| `internal/release` | `package release` | Own exclusive release-floor state and authenticate the accepted TUF-compatible H3 metadata profile, exact target identity, local binding, consecutive root rotation, build/protocol states, emergency transition, and durable non-decreasing floors. It returns a bounded Decision view and opaque authorization only for a verified accepted decision; it never downloads, installs, signs, exposes floor storage, repository administration, delegated targets, multi-repository maps, or ambient network/cache paths. | `github.com/theupdateframework/go-tuf/v2/metadata`, `github.com/theupdateframework/go-tuf/v2/metadata/trustedmetadata`, `golang.org/x/sys/windows` on Windows, standard library |
| `internal/release/custody` | `package custody` | Own one fixed local encrypted closed-alpha release seed record: five TUF top-level keys, one disclosure key, three H4-6A component keys, and one H4-4 corpus-authority key. `Initialize` and `Inspect` create or authenticate that record without exposing secrets. Its ADR-0052 `BuildAlphaInputs` seam accepts only one strict public alpha request, exact digest-bound Ubuntu Endpoint/control bytes, and a previously absent external directory; constructs the fixed initial 3-of-5 TUF and ACA1/ACS1 input set; exercises maintained Release, Network State, enrollment, and H4-6A inspection paths; then atomically publishes exactly the declared direct static files and a non-secret receipt. Its separate fixed `BuildAlphaSuccessor` seam validates the pinned complete RC1 static predecessor before secret use and can emit only the RC2 generation-2 metadata/catalog continuation retaining root generation 1. It never returns a decrypted seed, password, derived key, generic signer, arbitrary target/role/message, URL, upload operation, Endpoint execution, or Authority Vault state. | `internal/alphacontrol`, `internal/alphacontrol/inspection`, `internal/endpoint/enrollment`, `internal/network/state`, `internal/release`, `github.com/sigstore/sigstore/pkg/signature`, `github.com/theupdateframework/go-tuf/v2/metadata`, `golang.org/x/crypto/argon2`, standard library |
| `internal/update` | `package update` | Own one bounded compatible offline update transaction from a Release-issued opaque authorization through immutable staging, rollback reservation, stopped-runtime Adapter calls, atomic activation, self-test, committed journal evidence, idempotent replay, terminal inspection, and a caller-owned schema copy-on-write boundary without reading secrets or mutating Authority. Its D0 behavior fixture imports `internal/custody` only to create and commit a real encrypted Vault before the black-box transaction; maintained Update code receives neither the Vault nor its secret. | `internal/release`, `internal/custody` in D0 behavior tests only, `golang.org/x/sys/windows` on Windows, standard library |
