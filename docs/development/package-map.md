# Package map

This is the factual naming and dependency registry for maintained Go code. It
lists packages that exist, not future intentions. A new entry is an architecture
change: name the responsibility first, add the real Implementation and tests,
then update this table in the same change. The normative growth and dependency
rules are in [repository-layout.md](repository-layout.md).

R-091 retired the closed laboratory namespace. Its provenance is retained in
Git history; no current package may import or start a lab Module.

| Directory | Go declaration | Responsibility | May import |
|---|---|---|---|
| `cmd/ardents` | `package main` | Parse bounded Network Source, State, Entry, naming, enrollment, replacement, Service Instance, and Endpoint inputs, call their deep Modules, and render typed results. `service-instance initialize` creates or reopens one host-owned Instance generation and emits its stable public credential request to one explicit public file; `service-instance accept` durably accepts only the exact public Authority response. `endpoint headless` decodes its plan and delegates owner startup, Application transports, and shutdown to the Endpoint participant runtime; it makes no Service-authority or peer-selection decision. `endpoint open`, `publish`, and `withdraw` are thin callers of the shared local Application surfaces and receive no Browser, Route-plan, Target, or Grant input. Browser presentation is not part of this command or its dependency graph. | `internal/application/interfacev1/administration`, `internal/application/interfacev1/connection`, `internal/endpoint`, `internal/enrollment`, `internal/endpoint/portable`, `internal/endpoint/replacement`, `internal/entry`, `internal/naming`, `internal/naming/alpha`, `internal/naming/namespace` only for opaque Resolution views in behavior fixtures, `internal/naming/namespace/admission`, `internal/naming/namespace/authority`, `internal/naming/namespace/epoch`, `internal/naming/namespace/record`, `internal/naming/resolution`, `internal/network/duty`, `internal/network/state`, `internal/network/source`, `internal/release`, `internal/route`, `internal/service/instance`, `internal/service/publication` in behavior tests, standard library |
| `cmd/ardents-custody` | `package main` | Run the separate custody-process adapter. `create-service-authority` generates a Service Authority inside one encrypted active record; `issue-service-credential` requires the exact independently transferred host request digest before Vault/password access, advances only a bounded request, and renders its receipt only after same-directory staged no-replace publication or exact reopen of the public response; `inspect-envelope` is a pure public-header reader with no Vault root; `verify-record` consumes a password only through an interactive no-echo terminal; `export-recovery-bundle` and `restore-recovery-bundle` expose respectively a distinctly passworded test-restored Bundle and an authority-locked quarantine restore; `purge-record` deletes one exact verified encrypted record only after terminal confirmation while retaining its Authority floor. It never accepts Authority material or a password through arguments, environment, configuration, or shared application stdin; it does not activate a recovered Authority. | `internal/custody`, `internal/service/instance` in behavior tests, `golang.org/x/term`, standard library |
| `cmd/ardents-control` | `package main` | Run the separate enrolled alpha-control reader. `inspect-bundle` verifies the exact enrollment input/pin and advances one standalone inspection root with fixed catalog, Release, and Network children. `inspect-transitions` projects those exact verified facts into non-authorizing Release Safety, Network Epoch, Compatibility, and explicitly absent Namespace results while mutating no Endpoint state. `inspect-alpha-corpus` is a read-only diagnostic over explicit ACA2/corpus files and accepts no Endpoint root. `accept-alpha-corpus` is the sole corpus-floor mutator; it verifies the enrolled bundle, exact Endpoint artifact and running control companion, then verifies ACA1 plus ACA2/corpus evidence before advancing only the named Endpoint-local corpus floor. Caller-keyed low-level ACA1 inspection and future-public-claim projection are not command routes. | `internal/alphacontrol`, `internal/alphacontrol/inspection`, `internal/enrollment`, `internal/naming/alpha`, standard library |
| `cmd/ardents-node` | `package main` | Parse its own bounded Source or Node configuration and run one private distributor or separately keyed Node process. `issuer initialize` creates/reopens one owner-only Transit Grant issuer root and emits only its stable public profile; `issuer serve` runs that root through the normal State-owned Node lifecycle. Its runtime plan contains no signer, profile, Initiator, Route, or Target input. A Source may serve the maintained native State profile; State selects one runnable Rendezvous, Initiator, Introduction, Responder, or transit-issuance duty from explicit finite local reservations. Rendezvous alone may use an optional same-port loopback bind. Linux-only `contributor apply` remains restricted to the independently pinned dedicated-Rendezvous lifecycle. | `internal/contributor`, `internal/network/state`, `internal/network/source`, `internal/node`, `internal/resource` (diagnostic behavior test only), `internal/route`, `internal/route/credential`, standard library |
| `tests/e2e/endpoint` | `package endpoint` with external `endpoint_test` cases | E2E-only public-command evidence for the closed-alpha Enrollment Pin, enrollment-pinned reader, transition projection, alpha-corpus intake, and Endpoint lifecycle. It builds and executes the exact bundle artifact, accepts only its pinned local inventory, proves changed manifests fail before parsing, and owns its ephemeral canonical control/TUF fixture writers for Release Decision, Portable readiness, floor persistence, and clean-stop evidence. Linux cases additionally prove fixed-unit local replacement/recovery and direct-package install, distinct-version upgrade with fresh Release authorization, remove, and purge while protected state remains external. It never signs or releases a maintained bundle and proves neither participant provenance nor an enrolled network runtime. | `internal/alphacontrol`, `internal/alphacontrol/inspection`, `internal/naming/alpha`, `internal/release`, `github.com/sigstore/sigstore/pkg/signature`, `github.com/theupdateframework/go-tuf/v2/metadata`, standard library |
| `tests/e2e/service/fixturecommand/publish-app` | `package main` | E2E-only separately granted publication-operator fixture. It uses only the bounded local Service Administration socket and is built solely by the Endpoint recovery process test. | standard library |
| `tests/e2e/service/fixturecommand/stream-app` | `package main` | E2E-only paired direct opaque-stream workload used by retained qualification evidence. It owns deterministic bounded bytes and receipts but no Endpoint-local transport grammar. | standard library |
| `tests/e2e/node/fixturecommand/netem-relay` | `package main` | Carrier-qualification-only static TCP relay. It accepts one declared Linux netem mode (`delay`, 100% `drop`, or fixed delay/loss/reorder), applies it only to its own Docker `eth0` through a supplied `tc`, and forwards at most 1 MiB in each direction to one supplied upstream. It receives no Node State, credentials, Route, or Service input and is neither a maintained Node nor a Carrier implementation. | standard library |
| `internal/entry` | `package entry` | Own strict native-Profile, State-referenced Entry Invite v1 canonical issuance, decoding, and validation, including the Initiator-side current-fact verification and bounded durable EntryBinding replay ledger; a two-slot replacement/replay set; State-derived candidate lookup; and the bounded durable adjacent-contact lifecycle. Its owner retains every opened attachment cleanup lease, refuses new acquisition while closing, cancels and joins in-flight acquisition, durably records each terminal cleanup outcome, and only then releases its exclusive root lease. It never selects a Route, implements a carrier, retains a signer, or creates a User identity. | standard library; `golang.org/x/sys/windows` on Windows |
| `internal/alphacontrol` | `package alphacontrol` | Own the closed-alpha disclosure-catalog and component-statement readers, exact fixed component binding, separately enrollment-pinned Ed25519 disclosure/component-root verification, reader-owned catalog/component floors, and the non-authorizing domain-to-component transition declaration. It exposes no signing operation, reports reader-only inspection outcomes, and never authorizes Release, Network State, Namespace, Update, Route, or Endpoint behavior. | `golang.org/x/sys/windows` on Windows, standard library |
| `internal/contributor` | `package contributor` | Own one exact dedicated-host Rendezvous profile from independent manifest-pin verification before parsing through its closed binary/config inventory, fixed host paths and hardened systemd unit, generation-bound installation/update record, bounded READY diagnostics, supervisor-driven restart/drain/withdrawal, and exact confirmed removal. Readers accept the historical profile identity and normalize it to `ardents-rendezvous-dedicated-host-v1`; new records and reports emit only the canonical identity. It cannot enroll another duty, select Network State or peers, admit a public operator, preserve a removed identity, or claim capacity, availability, or independence. | standard library |
| `internal/alphacontrol/inspection` | `package inspection` | Compose one enrollment-pinned closed-alpha bundle with separate catalog, Release, and Network State inspection roots. It exposes only typed Release, Network, and Compatibility readers, verifies them against independently owned results, and emits one non-authorizing report; its ACA2 verifier can additionally require all accepted ACA1 components, then check one exact Alpha Name Corpus under the independently enrollment-pinned v2 corpus root. It exports no evidence writer and never starts an Endpoint or writes a corpus floor. | `internal/alphacontrol`, `internal/enrollment`, `internal/naming/alpha`, `internal/network/state`, `internal/release`, standard library |
| `internal/network/duty` | `package duty` | Own the bounded, owner-only, durable Endpoint-local Role Domain duty generations, watermark, expiry, conflict truth, and receiving-Node one-use Transit Grant spend ledger. It retains no issuer root, private generation, public profile, budget, request reservation, or signing operation; it owns no profile/request grammar, Route, Target, Service, or participant identity. | `golang.org/x/sys/windows` on Windows, standard library |
  | `internal/naming` | `package naming` | Own canonical Service Name parsing, normalization, and deterministic encoding checks as production-owned input gates. | standard library |
  | `internal/naming/alpha` | `package alpha` | Own the explicitly non-Namespace Alpha Service Link grammar and its finite signed Alpha Name Corpus: canonical alpha-only binding encoding, authority verification, expiry, authenticated total withdrawal, classified no-fallback outcomes, and one Endpoint-local owner-only durable signed-corpus serial/digest floor. Its lease precedes interrupted-write recovery and it never presents a result as the current canonical Namespace or as public registration. | `internal/naming`, `golang.org/x/sys/windows` on Windows, standard library |
  | `internal/naming/alpha/private` | `package private` | Own one alpha-only fixed-size OHTTP Client/Relay/Gateway exchange over an already verified finite Alpha Name Corpus. The Relay forwards only opaque bytes to its fixed Gateway; the Gateway receives the requested Alpha Service Link only after OHTTP decapsulation and returns the complete signed corpus. Neither retains per-request observations. The Client verifies corpus authority/cohort/network and decision-time validity before applying its supplied session or durable Endpoint-local floor; it never falls back to Target Links or canonical Namespace resolution. | `internal/naming`, `internal/naming/alpha`, reviewed OHTTP/CIRCL closure, standard library |
  | `internal/naming/namespace` | `package namespace` | Compose the authenticated Namespace modules and own only the opaque private-Resolution Gateway/verifier views. Concrete Namespace types and transitions belong to their cohesive submodules; root external-package tests exercise those owners directly. | `internal/naming/namespace/admission`, `internal/naming/namespace/authority`, `internal/naming/namespace/claim`, `internal/naming/namespace/epoch`, `internal/naming/namespace/record`, `internal/naming/namespace/recovery`, standard library |
| `internal/naming/namespace/admission` | `package admission` | Own one boot-scoped finite anonymous-work gate: authenticated challenges, bounded work, replay spending, expiry, capacity, and in-flight refusal. | standard library |
| `internal/naming/namespace/recovery` | `package recovery` | Own Recovery Policy validation, quorum-proof verification, and sealed Recovery Authorization facts. | `internal/naming`, standard library |
| `internal/naming/namespace/record` | `package record` | Own canonical Name Records, signature containers, lifecycle/lease transitions, parent lineage, and destination bindings. | `internal/naming`, `internal/naming/namespace/recovery`, standard library |
| `internal/naming/namespace/claim` | `package claim` | Own admitted root-claim commitment/reveal inputs, authenticated close verification, and one-use materialization of a winning root claim. | `internal/naming`, `internal/naming/namespace/admission`, `internal/naming/namespace/record`, standard library |
| `internal/naming/namespace/epoch` | `package epoch` | Own the compatible durable Namespace root, exact-successor pending journal and cursor, current/pending Epoch materialization, attestation, compact proof verification, and opaque current Name-Authority reconciliation witnesses. | `internal/naming`, `internal/naming/namespace/claim`, `internal/naming/namespace/record`, standard library |
| `internal/naming/namespace/authority` | `package authority` | Own canonical private control submissions and authorized transitions over admitted proofs, claim closes, record lifecycle, and recovery. It writes only already-authorized successors through the Epoch pending port; it owns no durable materialization queue. | `internal/naming`, `internal/naming/namespace/admission`, `internal/naming/namespace/claim`, `internal/naming/namespace/epoch`, `internal/naming/namespace/record`, `internal/naming/namespace/recovery`, standard library |
| `internal/naming/resolution` | `package resolution` | Own authenticated role selection, admitted immutable Binding exchange through Namespace and State Resolution views, fixed-size OHTTP Client/Relay/Gateway resolution and control exchange, finite replay rejection, observer-safe counters, and fail-closed local behavior without a plaintext fallback. It imports the root Namespace only for its owned Gateway/verifier views; concrete admission, control, claim-order, materialization, and Binding types come from their cohesive Namespace submodules. | `internal/naming/namespace`, `internal/naming/namespace/admission`, `internal/naming/namespace/authority`, `internal/naming/namespace/claim`, `internal/naming/namespace/epoch`, `internal/naming/namespace/record`, `internal/naming`, `internal/network/state`, reviewed OHTTP/CIRCL closure, standard library |
| `internal/network/source` | `package source` | Own the finite Direct-Origin Source plan, credential binding, private TLS transport, material selector, ordering, and exposure identity. | standard library |
| `internal/network/state` | `package state` | Own authenticated Network State acceptance, Epoch/View verification, commitments, deterministic role-domain assignment, materializations, current/pending decisions, finite acquisition, clock confidence, Direct Source duty retention, its exclusive durable root, bounded immutable generations, distribution journal, canonical byte cursor, atomic publication, and narrow private Resolution and Node-duty views. For an interactive Epoch, its private Resolution view projects exactly one State-assigned Destination Resolution Gateway, one `transit-issuance` issuer identity/family with its opaque signed profile bytes, and one indivisible Publisher attachment containing exactly one current Introduction, Rendezvous, and Responder; State neither parses owner-owned grammars nor ranks alternatives, and missing, conflicting, or ambiguous Publisher roles are unavailable. Product tests own independent canonical vector builders. | `internal/network/duty`, `internal/network/source`, `internal/resource`, `golang.org/x/sys/windows`, standard library |
| `tests/epochfixture/assignment` | `package assignment` | Independently construct deterministic role-domain assignments for black-box Network State fixtures; never serve maintained product code. | standard library |
| `tests/epochfixture/merkle` | `package merkle` | Independently construct canonical Epoch commitment roots and proofs for black-box Network State fixtures; never serve maintained product code. | standard library |
| `internal/route` | `package route` | Own native v1 volatile User-route orchestration and Attachment lifecycle behind `Open`, `Attach`, and `Close`: admission-before-work; State-authoritative exact Gateway/Initiator/issuer and Descriptor-authoritative Introduction/Rendezvous composition; private reachability and credential OHTTP exchanges through Entry; fixed Grant verification or Endpoint-adapted durable membership Grant presentation; exact RelaySetup/RelayReady, sealed Introduction, TLS carrier, cancellation, and joinable exactly-once cleanup. It returns only an authenticated opaque attachment and immutable evidence, never a complete Route plan, peer override, direct fallback, durable credential journal, or generic proxy. It also owns the closed EntryBinding, EndpointTransitBinding, Relay, and Introduction codecs plus Node-to-Node TCP/TLS or QUIC-v1 carriers. | `internal/entry`, `internal/network/state`, `internal/service/publication`, `internal/service/reachability`, `github.com/quic-go/quic-go`, standard library |
| `internal/route/credential` | `package credential` | Own the versioned membership-level Transit Grant issuance operation: a v2 owner-only root that privately retains the distinct purpose Grant signer/OHTTP material, returns only one stable Node-signed public profile declaring the exact permitted Initiator, and irreversibly binds its finite budget/idempotency ledger to the first authenticated State duty including canonical State Generation. It rejects every v1 root read-only without migration; a generation-only successor is unavailable before ledger work. It owns fixed target-free Request-ID request and `issued`/`exhausted`/`withdrawn`/`unavailable` outcome grammars, a one-use client, State-rechecking issuer Handler, and exact mutual-TLS HTTPS forwarder. It never receives a State private key or sees/selects a Name, Target, Publisher, Entry Invite, Route peer, or fallback. | `internal/route`, reviewed OHTTP/CIRCL closure, standard library |
| `internal/service/publication` | `package publication` | Own one exclusive C1 Service Instance publication root, monotonic generation floor, crash-atomic immutable public record, retained Instance signer, acquisition references, withdraw/supersede drain, and the Service-only Introduction instruction that binds one decrypted C-2 handoff to the exact current publication. It owns no local IPC client or historical ARIA receipt grammar. It copies and erases legacy raw Ed25519 input itself; an external non-exporting signer remains owned and erased by its host binding after publication drain. A restart retains only public proof and floor, never a live private Instance. | `golang.org/x/sys/windows` on Windows, standard library |
| `internal/service/instance` | `package instance` | Own one host-local Service Instance generation: owner-controlled durable root admission, exclusive process lifecycle, non-exporting Instance and Introduction private keys, fixed canonical public request/Authority-response handshake, exact at-most-once acceptance, and publication-floor reconciliation. A committed generation is durably redacted before runtime acknowledgement, remains live only through its opened binding, and becomes successor-required after restart; rejection, conflict, or withdrawal is terminal. It owns no Service Authority, Credential issuance, Route, Endpoint runtime, or Browser presentation. | `internal/service/publication`, `golang.org/x/sys/windows` on Windows, standard library |
| `internal/service/reachability` | `package reachability` | Own the closed Instance-signed Service Reachability Descriptor v1 fixed-Grant and v2 membership-Grant declarations; Gateway-local durable per-Target generation/conflict state; the signed, bounded GatewayProfile codec; and the fixed-size OHTTP Gateway/Client adapter for one opaque Target lookup. It verifies the Target-derived Authority, complete Publication, Instance signature, slot expiry, and bounded fields before Endpoint composition; Store atomically retains one accepted descriptor or a persistent same-generation conflict and refuses downgrade/overlapping higher generations across restart. Its one-purpose forwarder accepts only a bounded OHTTP envelope to one State-selected HTTPS Gateway and pins that Gateway's Ed25519 certificate key; it does not select State peers, discover a Gateway, acquire Entry, issue a Transit Grant, or create a Service Connection. | `internal/service/publication`, reviewed OHTTP/CIRCL closure, `golang.org/x/sys/windows` on Windows, standard library |
| `internal/service/connection` | `package connection` | Own immutable Service Connection facts — pinned Service Name provenance and finite recovery constraints — the closed unannounced native Service Connection v1 record grammar, and the logical stream lifecycle: ConnectionContext, exact Instance challenge/proof, attachment continuity, ordered/replayed bounded data, acknowledgement, replacement, explicit per-direction terminal outcome, and cleanup. It supports either exact declared workloads or orderly half-close before the caller's directional byte bounds. It accepts only opaque already-authenticated Attachments and an Application byte stream; it has no legacy reader, profile negotiation, direct fallback, publication private key, Application IPC authorization, or Route selection authority. | standard library |
| `internal/service/targetlink` | `package targetlink` | Own the closed canonical `ardents-target:v1:` text form: fixed Target algorithm identifier, exact network binding, and exact opaque Target bytes. It neither resolves reachability nor authenticates a remote Service Instance. | standard library |
| `internal/endpoint` | `package endpoint` | Own role-local Endpoint composition, participant runtime, Service-Link/Target/capability binding, and Application Connection/Administration Interfaces. It activates a Connection capability before protected work, then gives Route only the authenticated Target and accepts only Route's opaque verified Attachment/evidence. Endpoint owns separate durable at-most-once Introduction and Responder Transit Grant journals and supplies Route a narrow membership-credential callback; it never selects the relay carrier or User peers. The participant runtime retains one accepted non-exporting Service Instance binding, consumes State's exact Publisher attachment projection, and exposes publication only after the selected Introduction slot is ready. It owns no Route selection state, local client, wire grammar, Browser state, Browser presentation, or Firefox implementation. | `internal/alphacontrol`, `internal/alphacontrol/inspection`, `internal/application/interfacev1/administration`, `internal/application/broker`, `internal/application/interfacev1/connection`, `internal/route`, `internal/route/credential`, `internal/service/connection`, `internal/service/instance`, `internal/service/publication`, `internal/service/reachability`, `internal/service/targetlink`, `internal/entry`, `internal/naming/alpha`, `internal/naming/alpha/private`, `internal/network/duty`, `internal/network/state`, `internal/naming/namespace/record`, `golang.org/x/sys/windows` on Windows, standard library |
| `internal/enrollment` | `package enrollment` | Own independently pinned Network enrollment verification: `Verify` preserves the accepted v1-v3 Network inventory grammars and rejects Browser enrollment-v4, while `VerifyHeadless` additionally requires the manifest-pinned platform-named Node and Authority Custody companions. It pins the manifest before parsing and never executes or installs an artifact. | `internal/release`, standard library |
| `internal/endpoint/portable` | `package portable` | Own the per-user Portable Endpoint profile: final platform-resolved configuration/state/cache/runtime roots, owner-only path policy, one held state-owner lock, guarded stale filesystem-socket recovery, a bounded generic local attachment probe, visible lifecycle outcomes, and terminal cleanup. It creates neither an Application capability nor an Endpoint network route, and makes no same-user isolation claim. | `golang.org/x/sys/windows` on Windows, standard library |
| `internal/endpoint/replacement` | `package replacement` | Own the Ubuntu-only Endpoint-program binding between one opaque Release authorization and the exact executable permitted as a committed successor: owner-private current/prepared/journal records, held state lease, owner-only executable predecessor retention, bounded direct-file digest checks, same-directory atomic activation, interruption classification, and candidate-side no-network matching result. After an activated candidate self-test fails, it admits rollback only from that exact journal-bound retained predecessor copy and only with a fresh opaque Release authorization for the retained bytes; it owns no downloader, service-manager identifier, Endpoint route, Vault, Release floor store, or inferred local-byte rollback authorization. Its owner-level behavior tests verify that replacement does not mutate external Authority Vault or Release-floor bytes. | `internal/release`, `internal/custody` in behavior tests, `golang.org/x/sys/unix` on Linux, standard library |
| `internal/node` | `package node` | Own one bounded local Node identity credential, assignment admission, the retained private role-probe listener, the joined installed-Contributor clock observation, and native duties. Every new Entry or Transit Grant admission rechecks the current authenticated duty generation against the one that started the listener; a successor, conflict, expiry, or withdrawal invalidates old admission immediately. Transit issuance opens only an initialized owner root, verifies the byte-exact State profile and its single current Initiator candidate, binds the root to the first exact State duty, serves bounded mutual-TLS OHTTP, and stops on successor/withdrawal. Other duties retain State-supplied peer/Carrier/Entry facts: Rendezvous pairs authenticated legs; Initiator owns Entry admission and opaque Resolution/Credential relays; Introduction owns one-use transit admission and live slots; Responder owns Publisher-side admission and relay. Terminal lifecycle attempts a bounded join and reports role drain, listener/HTTP shutdown, and owner-root close; it cannot publish `WITHDRAWN` when cleanup fails or is unknown. The Node neither decrypts SealedIntroduction/relay OHTTP, discovers peers, selects fallback/capacity/a complete Route, nor receives a State private key. | `internal/entry`, `internal/network/duty`, `internal/resource`, `internal/route`, `internal/route/credential`, `internal/service/reachability`, standard library |
| `internal/resource` | `package resource` | Own Linux cgroup-v2/rlimit, Go-runtime, process-socket, and bounded managed-root storage measurement; retained legacy placements; the purpose-bound `ardents-rendezvous-dedicated-host-v1` placement; hysteresis; and the finite NORMAL/PROTECT/DRAIN pressure decision behind `Check` and `Observe`. Unsupported platforms explicitly refuse so consumers own readiness, admission, drain, and shutdown reactions without inventing resource policy. | standard library |
| `internal/architecture` | `package architecture` | Test repository structure, ownership inventory, package/import boundaries, artifact-lane separation, extraction rehearsals, names, dependencies, formatting, and quality wiring. | standard library |
| `internal/application/broker` | `package broker` | Own finite volatile Local Grant admission for explicit `connection` and `administration` surfaces used by the Network Endpoint: exact Principal-to-Grant matching, opaque one-use capability expiry, receipt commitments, and active Connection-session accounting. Connection activation consumes the capability without inheriting its pending TTL and returns only an opaque cancelable lease counted in the independent 64-session Connection budget; exact revoke and Broker close cancel matching active leases immediately, release is exactly once, and draining refuses new admission. Drain is available only when that exact Grant permitted it and the caller supplies a finite non-extendable deadline. Administration retains its separate six-capability consume-before-work budget and cannot consume Connection capacity. Its only isolation result is `generic/unqualified`; it has no sandbox, process-host, or qualified isolation authority, and is not Browser-only. | standard library |
| `internal/application/interfacev1/connection` | `package connection` | Own Application Interface v1 Connection contract, lifecycle, limits, typed errors/outcomes, stable AAI2 framing, conformance vectors, local server transport, and Application client. It accepts no State, Entry, Target, Route, Credential, Custody, Service key, administration fact, retry, or fallback. | standard library |
| `internal/application/interfacev1/administration` | `package administration` | Own the separately authorized Application Interface v1 Publish/Withdraw contract, lifecycle, closed grammar, failure response, conformance vectors, local server transport, and Application client. It carries no Connection bytes, Credential, Service key, Target, State, Route, peer, or Custody fact. | standard library |
| `internal/custody` | `package custody` | Own canonical `ardents-authority-envelope-v1` admission, fixed Argon2id/AES-GCM protection, exclusive encrypted Vault records, bounded per-operation secret use, pure public-only envelope inspection, distinct-password Recovery Bundle export with isolated test restore, authority-locked restore, and an exact non-decreasing local Authority floor. Record/floor writes flush ciphertext, use same-directory platform durable replacement, reopen before acknowledgement, and advance a floor only after encrypted publication; active verification requires exact floor agreement. Name signing and recovery remain bound to their authenticated Authority state. Service Authority creation generates its root inside custody and returns only the public Authority and derived Target; issuance requires an exact independently approved request digest, permits at most a 24-hour Credential within a 48-hour terminal horizon, and binds the request to a deterministic encrypted successor and monotonic public response before advancing the floor. Runtime Instance Keys and Local Grants remain outside custody. | `internal/naming/namespace/admission`, `internal/naming/namespace/authority`, `internal/naming/namespace/epoch`, `internal/naming/namespace/record`, `internal/service/instance`, `internal/service/publication`, `golang.org/x/crypto/argon2`, `golang.org/x/sys/windows` on Windows, standard library |
| `internal/release` | `package release` | Own exclusive release-floor state and authenticate the accepted closed-alpha TUF-compatible metadata profile, exact target identity, local binding, consecutive root rotation, build/protocol states, emergency transition, and durable non-decreasing floors. It returns a bounded Decision view and opaque authorization only for a verified accepted decision; it never downloads, installs, signs, exposes floor storage, repository administration, delegated targets, multi-repository maps, or ambient network/cache paths. Existing stage-derived metadata identifiers remain an explicit compatibility obligation pending a separately researched migration. | `github.com/theupdateframework/go-tuf/v2/metadata`, `github.com/theupdateframework/go-tuf/v2/metadata/trustedmetadata`, `golang.org/x/sys/windows` on Windows, standard library |
