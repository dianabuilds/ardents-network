# Package map

This is the factual naming and dependency registry for maintained Go code. It
lists packages that exist, not future intentions. A new entry is an architecture
change: name the responsibility first, add the real Implementation and tests,
then update this table in the same change. The normative growth and dependency
rules are in [repository-layout.md](repository-layout.md).

Every package below `internal/lab/` is laboratory code. The path prefix is the
executable classification: product packages and commands cannot import it.
Only purpose-named `cmd/*-lab` adapters may start laboratory Modules.
`internal/lab` itself contains no Go files and is not a package.

| Directory | Go declaration | Responsibility | May import |
|---|---|---|---|
| `cmd/ardents` | `package main` | Parse bounded offline or Direct-Origin Source inputs, call Network State, run one role-local Endpoint process through `endpoint run`, import one State-referenced signed Entry Invite through `entry import`, or execute selected naming operations through `name`, rendering bounded receipts. | `internal/endpoint`, `internal/entry`, `internal/naming`, `internal/naming/namespace` and `internal/naming/resolution` in command behavior-test fixtures, `internal/network/duty`, `internal/network/state`, `internal/network/source`, `internal/planfile`, standard library |
| `cmd/ardents-custody` | `package main` | Run the separate custody-process adapter. `inspect-envelope` renders only public canonical envelope-header facts; `verify-record` consumes a password only through an interactive no-echo terminal and renders bounded non-secret verification facts. It never accepts Authority material or a password through arguments, environment, configuration, or shared application stdin. | `internal/custody`, `golang.org/x/term`, standard library |
| `cmd/ardents-node` | `package main` | Parse one bounded source or Node configuration and run one private distributor or separately keyed Node process. | `internal/network/state`, `internal/network/source`, `internal/node`, `internal/planfile`, standard library |
| `cmd/ardents-stream-app` | `package main` | Act as one external opaque-stream tracer Application using its supplied local byte stream and classified Connection Result, or as the paired direct TCP workload used to measure live carrier impairment. | `internal/endpoint`, `internal/streamworkload`, standard library |
| `cmd/ardents-publish-app` | `package main` | Act as one separately granted external publication operator using only its supplied Service Administration socket. | standard library |
| `cmd/ardents-release` | `package main` | Accept one offline-import TUF-compatible H3 release decision or compose its opaque accepted authorization with one stopped-runtime offline Update Transaction, then print only the corresponding bounded JSON result. | `internal/release`, `internal/update`, standard library |
| `cmd/carrier-lab` | `package main` | Parse fixed Carrier Lab commands, call the selected Module, and translate results to exit codes. | `internal/lab/directcontrol`, `internal/lab/carrier`, `internal/lab/tooling`, `internal/lab/nativecircuit`, `internal/lab/preflight`, `internal/lab/routecomparison`, standard library |
| `cmd/named-site-lab` | `package main` | Parse fixed Gate C commands, derive one experiment identity, call `namedsite`, and translate its result to an exit code. | `internal/lab/runlayout`, `internal/lab/namedsite`, standard library |
| `cmd/blocked-entry-verify-lab` | `package main` | Parse immutable blocked-entry evidence, call the separately built independent verifier, and render its canonical result for development or S9.6 qualification. | `internal/lab/blockedverify`, standard library |
| `cmd/stage6-evidence-lab` | `package main` | Run exactly one bounded A0-D6 S6E1 development campaign and publish immutable manifest, private, observation, terminal, and cleanup artifacts without a verdict. | `internal/lab/stage6evidence`, standard library |
| `cmd/stage6-verify-lab` | `package main` | Independently admit one immutable S6E1 campaign, recompute every required predicate, and publish canonical `pass`, `fail`, or `invalid`. | `internal/lab/stage6verify`, `internal/lab/stage6evidence` in command behavior tests, standard library |
| `internal/lab/carrier` | `package carrier` | Own the fixed two-role Carrier Lab isolation scenario: role configuration, Compose lifecycle, bounded observations, fault injection, verdict, and cleanup. | `internal/lab/preflight`, standard library |
| `internal/lab/tooling` | `package tooling` | Verify the exact external tool supply and runnable image pair; own shaping, capture, tracer, bounded live pressure, fail-closed smoke, and evidence behind `VerifyInputs`, `VerifyNativeImages`, `RunSmoke`, `RunRole`, `RunNativeRole`, and `RunMemoryPressure`. | `internal/lab/sourceidentity`, `internal/lab/preflight`, standard library |
| `internal/lab/preflight` | `package preflight` | Orchestrate pinned Carrier Lab setup and write canonical preflight evidence and cleanup verdicts for a verified experiment run. | `internal/lab/sourceidentity`, `internal/lab/runlayout`, standard library |
| `internal/lab/runlayout` | `package runlayout` | Own and revalidate the filesystem identity and derived paths of one maintained laboratory run. | standard library |
| `internal/lab/sourceidentity` | `package sourceidentity` | Bind all maintained experiment code, tests, workflows, and container inputs into one shared source SHA-256. | standard library |
| `internal/lab/namedsite` | `package namedsite` | Own the bounded Gate C Named Unlisted Site scenario, role processes, security decisions, evidence, and cleanup. | `internal/lab/sourceidentity`, `internal/lab/runlayout`, `internal/lab/nativecircuit`, standard library; reviewed OHTTP closure |
| `internal/lab/directcontrol` | `package directcontrol` | Own the complete lab-only Direct TLS fixture, control lifecycle, roles, protected-record fault, evidence, and cleanup behind `RunControl`, `RunRole`, and `RunTamper`; never act as a Route or fallback. | `internal/lab/preflight`, standard library |
| `internal/lab/nativecircuit` | `package nativecircuit` | Own the fixed lab-only native C-5/C2 candidate: bounded wire protocol, HPKE Introduction, telescoped Node TLS, joined endpoint TLS, opaque UDS attachment, role-local runtime, Compose lifecycle, evidence, and cleanup behind its small run/role interface. | `internal/lab/runlayout`, `internal/lab/tooling`, `internal/lab/preflight`, standard library |
| `internal/lab/routecomparison` | `package routecomparison` | Own the frozen R-013 comparative sequence, immutable workload/seed manifest, coarse statistics, conjunctive C-5 verdict, retained evidence, and cleanup behind `Run`. | `internal/lab/sourceidentity`, `internal/lab/nativecircuit`, `internal/lab/preflight`, standard library |
| `internal/lab/blockedverify` | `package blockedverify` | Independently recompute one canonical blocked-entry `pass`, `fail`, or `invalid` result from immutable manifests, observations, private canaries, secret commitments, and cleanup inventory behind `Verify`. | `golang.org/x/sys/unix` on Linux, `golang.org/x/sys/windows` on Windows, standard library |
| `internal/lab/stage6evidence` | `package stage6evidence` | Execute the exact bounded A0-D6 S6E1 corpus through maintained Stage 6 seams and publish immutable artifacts without verdict authority behind `Run`. | `internal/application/broker`, `internal/endpoint`, `internal/naming/namespace`, `internal/naming/resolution`, `internal/naming`, `internal/network/state`, standard library |
| `internal/lab/stage6verify` | `package stage6verify` | Independently admit S6E1 roots and recompute exact lifecycle, authority, recovery, claim, admission, connection, role-separation, no-fallback, and cleanup predicates behind `Stage6Verifier.Verify`. | `internal/lab/stage6evidence` in behavior tests, `internal/naming/namespace` in mutation tests, standard library |
| `internal/lab/modulecache` | `package modulecache` | Generate one canonical external Stage 5 Go module-cache archive from an empty cache, the exact module graph, bounded tool processes, and atomic external publication behind `Generate`. | standard library |
| `internal/entry` | `package entry` | Own strict native-Profile Entry Invite v1 decoding/validation, including the Initiator-side current-fact verification and bounded durable EntryBinding replay ledger; a two-slot replacement/replay set; a bounded durable adjacent-contact lifecycle with restart settlement; and State-derived candidate lookup. It never selects a Route, implements a carrier, or creates a User identity. | standard library; `golang.org/x/sys/windows` on Windows |
| `internal/network/duty` | `package duty` | Own the bounded, owner-only, durable Endpoint-local Role Domain duty generations, watermark, expiry, and conflict truth used by maintained role producers. | `golang.org/x/sys/windows` on Windows, standard library |
| `internal/naming` | `package naming` | Own Stage 6 canonical Service Name parsing, normalization, and deterministic encoding checks as production-owned input gates. | standard library |
| `internal/naming/namespace` | `package namespace` | Compose the authenticated Namespace modules, retain the opaque private-Resolution Gateway/verifier views, and provide the explicitly retained C4 compatibility façade while callers migrate. | `internal/naming/namespace/admission`, `internal/naming/namespace/authority`, `internal/naming/namespace/claim`, `internal/naming/namespace/epoch`, `internal/naming/namespace/record`, `internal/naming/namespace/recovery`, standard library |
| `internal/naming/namespace/admission` | `package admission` | Own one boot-scoped finite anonymous-work gate: authenticated challenges, bounded work, replay spending, expiry, capacity, and in-flight refusal. | standard library |
| `internal/naming/namespace/recovery` | `package recovery` | Own Recovery Policy validation, quorum-proof verification, and sealed Recovery Authorization facts. | `internal/naming`, standard library |
| `internal/naming/namespace/record` | `package record` | Own canonical Name Records, signature containers, lifecycle/lease transitions, parent lineage, and destination bindings. | `internal/naming`, `internal/naming/namespace/recovery`, standard library |
| `internal/naming/namespace/claim` | `package claim` | Own admitted root-claim commitment/reveal inputs, authenticated close verification, and one-use materialization of a winning root claim. | `internal/naming`, `internal/naming/namespace/admission`, `internal/naming/namespace/record`, standard library |
| `internal/naming/namespace/epoch` | `package epoch` | Own the compatible durable Namespace root, exact-successor pending journal and cursor, current/pending Epoch materialization, attestation, compact proof verification, and opaque current Name-Authority reconciliation witnesses. | `internal/naming`, `internal/naming/namespace/claim`, `internal/naming/namespace/record`, standard library |
| `internal/naming/namespace/authority` | `package authority` | Own canonical private control submissions and authorized transitions over admitted proofs, claim closes, record lifecycle, and recovery. It writes only already-authorized successors through the Epoch pending port; it owns no durable materialization queue. | `internal/naming`, `internal/naming/namespace/admission`, `internal/naming/namespace/claim`, `internal/naming/namespace/epoch`, `internal/naming/namespace/record`, `internal/naming/namespace/recovery`, standard library |
| `internal/naming/resolution` | `package resolution` | Own authenticated role selection, admitted immutable Binding exchange through Namespace and State Resolution views, fixed-size OHTTP Client/Relay/Gateway resolution and control exchange, finite replay rejection, observer-safe counters, and fail-closed local behavior without a plaintext fallback. | `internal/naming/namespace`, `internal/naming`, `internal/network/state`, reviewed OHTTP/CIRCL closure, standard library |
| `internal/network/source` | `package source` | Own the finite Direct-Origin Source plan, credential binding, private TLS transport, material selector, ordering, and exposure identity. | standard library |
| `internal/network/state` | `package state` | Own authenticated Network State acceptance, Epoch/View verification, commitments, deterministic role-domain assignment, materializations, current/pending decisions, finite acquisition, clock confidence, Direct Source duty retention, its exclusive durable root, bounded immutable generations, distribution journal, canonical byte cursor, atomic publication, and narrow private Resolution and Node-duty views. Product tests own independent canonical vector builders. | `internal/network/duty`, `internal/network/source`, `internal/resource`, standard library |
| `tests/epochfixture/assignment` | `package assignment` | Independently construct deterministic role-domain assignments for black-box Network State fixtures; never serve maintained product code. | standard library |
| `tests/epochfixture/merkle` | `package merkle` | Independently construct canonical Epoch commitment roots and proofs for black-box Network State fixtures; never serve maintained product code. | standard library |
| `internal/route` | `package route` | Own native v1 endpoint selection and volatile Route Attachment lifecycle behind `Open`, `Attach`, and `Close`: caller-owned resource reservation, State-pinned User-to-Initiator TLS, concurrent-selection exclusion, bounded cancellation, and closed EntryBinding/Node-to-Node LegBinding/SealedIntroduction codecs. It exposes only an attachment byte carrier, never a complete Route plan. It has no H3 reader, peer runtime, Node profile, or peer announcement. | `internal/entry`, `internal/network/state`, standard library |
| `internal/service/publication` | `package publication` | Own one exclusive C1 Service Instance publication root, monotonic generation floor, crash-atomic immutable public record, volatile Instance signer, acquisition references, withdraw/supersede drain, and private-material erasure behind `Open`, `Publish`, `AcquireAt`, `Unpublish`, and `Close`. A restart retains only public proof and floor, never a live private Instance. | `golang.org/x/sys/windows` on Windows, standard library |
| `internal/service/connection` | `package connection` | Own immutable Service Connection facts — pinned Service Name provenance and finite recovery constraints — the closed unannounced native Service Connection v1 record grammar, and the logical stream lifecycle: ConnectionContext, exact Instance challenge/proof, attachment continuity, ordered/replayed bounded data, acknowledgement, replacement, terminal outcome, and cleanup. It accepts only opaque already-authenticated Attachments and an Application byte stream; it has no legacy reader, profile negotiation, direct fallback, publication private key, Application IPC authorization, or Route selection authority. | standard library |
| `internal/endpoint` | `package endpoint` | Own role-local Endpoint process composition, strict bounded plan loading, the one volatile Broker generation, scoped Application/administration IPC, direct Publication lease/TLS/exact-Instance carrier composition, native Connection invocation, raw Application data and one classified terminal Result, reusable Route Attachment acceptance, readiness, and resource-observed cleanup behind `Run`. It admits only the fixed native Service Connection Profile in a recovery plan. Its sole v1 Application contract explicitly selects a sideband terminal-result channel before opaque bytes; raw-tail and timing-selected fallback are C0 retired. It makes no qualified isolation claim. | `internal/application/broker`, `internal/planfile`, `internal/service/connection`, `internal/service/publication`, `internal/naming/namespace` in behavior tests, standard library |
| `internal/streamworkload` | `package streamworkload` | Generate, stream, pace, and validate deterministic bounded opaque workloads for external Application tracers, including the paired direct carrier baseline, without payload-sized allocation. | standard library |
| `internal/node` | `package node` | Own one bounded local Node identity credential, assignment admission, private authenticated role-probe listener/TLS/framing/replay/capacity, readiness, pressure reaction, durable Endpoint-local duty, drain, withdrawal, and terminal cleanup from narrow authenticated facts. | `internal/network/duty`, `internal/planfile`, `internal/resource`, standard library |
| `internal/planfile` | `package planfile` | Own bounded operator-plan and credential reads, strict JSON decoding, exact hexadecimal fields, and TLS key-pair loading. | standard library |
| `internal/resource` | `package resource` | Own Linux cgroup-v2/rlimit and Go-runtime measurement, fixed H3 placement, hysteresis, and the finite NORMAL/PROTECT/DRAIN pressure decision behind `Check` and `Observe`; unsupported platforms explicitly refuse so consumers own readiness, admission, drain, and shutdown reactions without inventing resource policy. | standard library |
| `internal/architecture` | `package architecture` | Test repository structure, names, dependencies, formatting, and quality wiring. | standard library |
| `internal/application/broker` | `package broker` | Own finite volatile Local Grant admission for explicit `connection` and `administration` surfaces: exact Principal-to-Grant matching, opaque one-use capabilities, expiry, receipt commitments, immediate grant revocation, and terminal close. A finite drain may affect only a Grant explicitly configured to permit it. Its only isolation result is `generic/unqualified`; it has no sandbox, process-host, or qualified isolation authority. | standard library |
| `internal/custody` | `package custody` | Own canonical `ardents-authority-envelope-v1` admission, fixed Argon2id/AES-GCM protection, exclusive encrypted Vault records, bounded per-operation secret use, public-only envelope inspection, distinct-password Recovery Bundle export with isolated test restore, confirmed replacement that preserves the old encrypted Bundle on ordinary publication/test failure, encrypted authority-locked export-only restore into a previously empty Vault quarantine root, and an exact non-decreasing local Authority floor. Record/floor writes flush ciphertext, use same-directory platform durable replacement, reopen before acknowledgement, and advance a floor only after encrypted publication; active verification requires exact floor agreement. A locked Name Bundle may activate only from a fresh opaque witness of one current active Namespace Authority strictly above its stored generation/revision; activation durably creates a higher-watermark active successor and floor before a signature can occur. An active Name record can sign one exact sealed transition or one Namespace-derived transition/successor-Record pair only when its Authority key, generation, and revision match the active record, without exposing its root. Runtime Instance Keys and Local Grants remain outside custody. | `internal/naming/namespace/authority`, `internal/naming/namespace/epoch`, `internal/naming/namespace/record`, `internal/naming/namespace` in behavior tests, `golang.org/x/crypto/argon2`, `golang.org/x/sys/windows` on Windows, standard library |
| `internal/release` | `package release` | Own exclusive release-floor state and authenticate the accepted TUF-compatible H3 metadata profile, exact target identity, local binding, consecutive root rotation, build/protocol states, emergency transition, and durable non-decreasing floors. It returns a bounded Decision view and opaque authorization only for a verified accepted decision; it never downloads, installs, signs, exposes floor storage, repository administration, delegated targets, multi-repository maps, or ambient network/cache paths. | `github.com/theupdateframework/go-tuf/v2/metadata`, `github.com/theupdateframework/go-tuf/v2/metadata/trustedmetadata`, `golang.org/x/sys/windows` on Windows, standard library |
| `internal/update` | `package update` | Own one bounded compatible offline update transaction from a Release-issued opaque authorization through immutable staging, rollback reservation, stopped-runtime Adapter calls, atomic activation, self-test, committed journal evidence, idempotent replay, terminal inspection, and a caller-owned schema copy-on-write boundary without reading secrets or mutating Authority. Its D0 behavior fixture imports `internal/custody` only to create and commit a real encrypted Vault before the black-box transaction; maintained Update code receives neither the Vault nor its secret. | `internal/release`, `internal/custody` in D0 behavior tests only, `golang.org/x/sys/windows` on Windows, standard library |
## Carrier Lab command registry

| Command | Owning Module | Fixed responsibility |
|---|---|---|
| `bootstrap` | `internal/lab/preflight` | Orchestrate pinned host/container setup while reserving canonical evidence and verdicts for the pinned verifier. |
| `evaluate` | `internal/lab/preflight` | Verify prepared pinned inputs and write intermediate preflight evidence. |
| `finalize-cleanup` | `internal/lab/preflight` | Verify owned-resource removal and publish the final preflight verdict. |
| `compose-smoke` | `internal/lab/carrier` | Run the fixed two-role isolated scenario and own its lifecycle and summary. |
| `smoke-role` | `internal/lab/carrier` | Run one data-only role whose config names only its allowed peer. |
| `tooling-verify` | `internal/lab/tooling` | Verify the exact external `.deb` set against the committed tool lock. |
| `tooling-smoke` | `internal/lab/tooling` | Bind the runnable image to base/lock/source/binary identity, then own real shaping/capture, exact topology evidence, fail-closed verdict, and cleanup. |
| `tooling-role` | `internal/lab/tooling` | Run one fixed synthetic tracer, shaper, or capture role. |
| `direct-control` | `internal/lab/directcontrol` | Generate one ephemeral Target/Instance fixture and run the positive and fixed Direct TLS negative cases. |
| `direct-role` | `internal/lab/directcontrol` | Run one User or Service tracer role for the fixed Direct TLS control. |
| `direct-tamper` | `internal/lab/directcontrol` | Modify one TLS-protected record without receiving endpoint or Application knowledge. |
| `native-run` | `internal/lab/nativecircuit` | Run the fixed isolated native C-5/C2 development smoke and retain its bounded verdict. |
| `native-role` | `internal/lab/nativecircuit` | Run one role-local native User, Service, relay, Rendezvous, or Introduction process. |
| `native-tool-role` | `internal/lab/tooling` | Apply real link shaping or capture for one native role namespace with one exact capability. |
| `native-negative` | `internal/lab/nativecircuit` | Execute one fixed fail-closed R-013 negative inside the immutable application image. |
| `route-experiment` | `internal/lab/routecomparison` | Run the frozen Direct/C-3/C-5 comparison, required negatives, conditional Tor reference, and canonical verdict without building or downloading. |
| `pressure-memory` | `internal/lab/tooling` | Hold one bounded resident allocation, optionally alongside an incomplete local TCP setup, to exercise real container pressure and admission. |

The architecture gate parses this table and rejects an unregistered or stale
package, a mismatched Go declaration, or a current project import absent from
the owning row's `May import` column.
