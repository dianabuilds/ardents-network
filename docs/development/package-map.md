# Package map

This is the factual naming and dependency registry for maintained Go code. It
lists packages that exist, not future intentions. A new entry is an architecture
change: name the responsibility first, add the real Implementation and tests,
then update this table in the same change. The normative growth and dependency
rules are in [repository-layout.md](repository-layout.md).

Every package below `internal/lab/` is laboratory code. The path prefix is the
executable classification: product packages and commands cannot import it.
Only purpose-named `cmd/*-lab` adapters may start laboratory Modules.
`internal/lab` itself contains no Go files and is not a package.

| Directory | Go declaration | Responsibility | May import |
|---|---|---|---|
| `cmd/ardents` | `package main` | Parse bounded offline or Direct-Origin Source inputs, call Network State, and render the accepted generation. | `internal/network/state`, `internal/network/source`, `internal/planfile`, standard library |
| `cmd/ardents-name` | `package main` | Validate canonical Service Name/Record encodings or execute one admitted private resolution or private naming control operation selected from recovered authenticated Network State, emitting bounded provenance/terminal receipts without fallback. | `internal/nameadmission`, `internal/nameauthority` in command behavior tests; `internal/naming`, `internal/namelease`, `internal/nameresolution`, `internal/namestore` in command behavior tests, `internal/network/state`, `internal/planfile`, standard library |
| `cmd/ardents-bridge` | `package main` | Import Bridge Invites or compose one authenticated Bridge-domain Node lifecycle with the selected candidate server and its manifest-bound next Initiator leg. | `internal/bridge`, `internal/camouflage`, `internal/network/duty`, `internal/network/state`, `internal/node`, `internal/node/probe`, `internal/planfile`, `internal/routeplan`, standard library |
| `cmd/ardents-node` | `package main` | Parse one bounded source or Node configuration and run one private distributor or separately keyed Node process. | `internal/network/state`, `internal/network/source`, `internal/node`, `internal/node/probe`, `internal/planfile`, standard library |
| `cmd/ardents-route` | `package main` | Load one role-local Route sequence, compose an optional inherited-transition Bridge carrier for a client actor, and render bounded observations. | `internal/bridge`, `internal/camouflage`, `internal/network/duty`, `internal/network/state`, `internal/planfile`, `internal/routeplan`, standard library |
| `cmd/ardents-service` | `package main` | Adapt one role-local Endpoint process invocation to bounded JSON readiness and terminal output. | `internal/serviceendpoint`, standard library |
| `cmd/ardents-stream-app` | `package main` | Act as one external opaque-stream tracer Application using its supplied local byte stream and classified Connection Result, or as the paired direct TCP workload used to measure live carrier impairment. | `internal/applicationipc`, `internal/streamworkload`, standard library |
| `cmd/ardents-publish-app` | `package main` | Act as one separately granted external publication operator using only its supplied Service Administration socket. | standard library |
| `cmd/ardents-release` | `package main` | Accept one offline-import TUF-compatible H3 release decision or compose its opaque accepted authorization with one stopped-runtime offline Update Transaction, then print only the corresponding bounded JSON result. | `internal/release`, `internal/updatetransaction`, standard library |
| `cmd/carrier-lab` | `package main` | Parse fixed Carrier Lab commands, call the selected Module, and translate results to exit codes. | `internal/lab/directcontrol`, `internal/lab/carrier`, `internal/lab/tooling`, `internal/lab/nativecircuit`, `internal/lab/preflight`, `internal/lab/routecomparison`, standard library |
| `cmd/named-site-lab` | `package main` | Parse fixed Gate C commands, derive one experiment identity, call `namedsite`, and translate its result to an exit code. | `internal/lab/runlayout`, `internal/lab/namedsite`, standard library |
| `cmd/blocked-entry-lab` | `package main` | Run bounded Stage 5 development evidence or prepare the external frozen R-037 specification retained for S9.6 qualification, without authoring a verdict. | `internal/lab/blockedentry`, standard library |
| `cmd/blocked-entry-verify-lab` | `package main` | Parse immutable blocked-entry evidence, call the separately built independent verifier, and render its canonical result for development or S9.6 qualification. | `internal/lab/blockedverify`, standard library |
| `cmd/stage6-evidence-lab` | `package main` | Run exactly one bounded A0-D6 S6E1 development campaign and publish immutable manifest, private, observation, terminal, and cleanup artifacts without a verdict. | `internal/lab/stage6evidence`, standard library |
| `cmd/stage6-verify-lab` | `package main` | Independently admit one immutable S6E1 campaign, recompute every required predicate, and publish canonical `pass`, `fail`, or `invalid`. | `internal/lab/stage6verify`, `internal/lab/stage6evidence` in command behavior tests, standard library |
| `internal/lab/carrier` | `package carrier` | Own the fixed two-role Carrier Lab isolation scenario: role configuration, Compose lifecycle, bounded observations, fault injection, verdict, and cleanup. | `internal/lab/preflight`, standard library |
| `internal/lab/tooling` | `package tooling` | Verify the exact external tool supply and runnable image pair; own shaping, capture, tracer, bounded live pressure, fail-closed smoke, and evidence behind `VerifyInputs`, `VerifyNativeImages`, `RunSmoke`, `RunRole`, `RunNativeRole`, and `RunMemoryPressure`. | `internal/lab/sourceidentity`, `internal/lab/preflight`, standard library |
| `internal/lab/preflight` | `package preflight` | Orchestrate pinned Carrier Lab setup and write canonical preflight evidence and cleanup verdicts for a verified experiment run. | `internal/lab/sourceidentity`, `internal/lab/runlayout`, standard library |
| `internal/lab/runlayout` | `package runlayout` | Own and revalidate the filesystem identity and derived paths of one maintained laboratory run. | standard library |
| `internal/lab/sourceidentity` | `package sourceidentity` | Bind all maintained experiment code, tests, workflows, and container inputs into one shared source SHA-256. | standard library |
| `internal/lab/namedsite` | `package namedsite` | Own the bounded Gate C Named Unlisted Site scenario, role processes, security decisions, evidence, and cleanup. | `internal/lab/sourceidentity`, `internal/lab/runlayout`, `internal/lab/nativecircuit`, standard library; reviewed OHTTP closure |
| `internal/lab/directcontrol` | `package directcontrol` | Own the complete lab-only Direct TLS fixture, control lifecycle, roles, protected-record fault, evidence, and cleanup behind `RunControl`, `RunRole`, and `RunTamper`; never act as a Route or fallback. | `internal/lab/preflight`, standard library |
| `internal/lab/nativecircuit` | `package nativecircuit` | Own the fixed lab-only native C-5/C2 candidate: bounded wire protocol, HPKE Introduction, telescoped Node TLS, joined endpoint TLS, opaque UDS attachment, role-local runtime, Compose lifecycle, evidence, and cleanup behind its small run/role interface. | `internal/lab/runlayout`, `internal/lab/tooling`, `internal/lab/preflight`, standard library |
| `internal/lab/routecomparison` | `package routecomparison` | Own the frozen R-013 comparative sequence, immutable workload/seed manifest, coarse statistics, conjunctive C-5 verdict, retained evidence, and cleanup behind `Run`. | `internal/lab/sourceidentity`, `internal/lab/nativecircuit`, `internal/lab/preflight`, standard library |
| `internal/lab/blockedentry` | `package blockedentry` | Own bounded Stage 5 evidence and deferred S9.6 R-037 specification preparation, private canary and secret artifacts, immutable manifests, canonical observations, fault injection, and complete cleanup inventory behind `Run`; never author a verdict. | `internal/lab/sourceidentity`, `golang.org/x/sys/unix` on Linux, `golang.org/x/sys/windows` on Windows, standard library |
| `internal/lab/blockedverify` | `package blockedverify` | Independently recompute one canonical blocked-entry `pass`, `fail`, or `invalid` result from immutable manifests, observations, private canaries, secret commitments, and cleanup inventory behind `Verify`. | `golang.org/x/sys/unix` on Linux, `golang.org/x/sys/windows` on Windows, standard library |
| `internal/lab/stage6evidence` | `package stage6evidence` | Execute the exact bounded A0-D6 S6E1 corpus through maintained Stage 6 seams and publish immutable artifacts without verdict authority behind `Run`. | `internal/nameadmission`, `internal/nameauthority`, `internal/nameclaim`, `internal/namelease`, `internal/namerecovery`, `internal/nameresolution`, `internal/namestore`, `internal/naming`, `internal/network/state`, `internal/serviceconn`, standard library |
| `internal/lab/stage6verify` | `package stage6verify` | Independently admit S6E1 roots and recompute exact lifecycle, authority, recovery, claim, admission, connection, role-separation, no-fallback, and cleanup predicates behind `Stage6Verifier.Verify`. | `internal/lab/stage6evidence` in behavior tests, `internal/namelease` in mutation tests, standard library |
| `internal/lab/modulecache` | `package modulecache` | Generate one canonical external Stage 5 Go module-cache archive from an empty cache, the exact module graph, bounded tool processes, and atomic external publication behind `Generate`. | standard library |
| `internal/bridge` | `package bridge` | Own strict Bridge Invite validation, the two-slot single-replacement Entry Set, the complete durable attempt/contact sequence behind one candidate-opener seam, replay history, restart revalidation, retained attempt evidence, and atomic bounded persistence. | `internal/network/state`, `golang.org/x/sys/windows` on Windows, standard library |
| `internal/camouflage` | `package camouflage` | Validate the selected WebTunnel envelope and own its pinned client/server subprocesses, SOCKS carrier, TLS/HTTP front, admission control, and bounded cleanup. | standard library |
| `internal/network/duty` | `package duty` | Own the bounded, owner-only, durable Endpoint-local Role Domain duty generations, watermark, expiry, and conflict truth used by maintained role producers. | `golang.org/x/sys/windows` on Windows, standard library |
| `internal/naming` | `package naming` | Own Stage 6 canonical Service Name parsing, normalization, and deterministic encoding checks as production-owned input gates. | standard library |
| `internal/namelease` | `package namelease` | Own Name Lease, Name Generation, canonical Name Record encoding, parent binding, monotonic Target/authority transitions, and delayed recovery lifecycle invariants. It consumes only sealed threshold facts and does not verify signatures or own ordering. | `internal/namerecovery`, `internal/naming`, standard library |
| `internal/nameauthority` | `package nameauthority` | Own the accepted Ed25519 Name Authority identity, domain/network-bound canonical Record containers, anonymous-admission composition, strict private-control execution, exact predecessor/parent transitions, sealed threshold-recovery application, and authenticated root-claim materialization. | `internal/nameadmission`, `internal/nameclaim`, `internal/namelease`, `internal/namerecovery`, `internal/naming`, standard library |
| `internal/nameclaim` | `package nameclaim` | Own the accepted authenticated two-Epoch root-claim commit/reveal set, bounded canonical control proof, deterministic input-ordinal ordering, and explicit conflict/fork/unavailable classifications without persistence. | `internal/naming`, standard library |
| `internal/nameadmission` | `package nameadmission` | Own the accepted four-surface O1b anonymous-work profile, boot-scoped HMAC challenges, one-use verification, and bounded no-queue spent/in-flight state without identity, payment, or fairness claims. | standard library |
| `internal/namerecovery` | `package namerecovery` | Own the accepted bounded generation-scoped Recovery Policy, canonical initiation/cancellation transcripts, and threshold verification of distinct Recovery Authority signatures without Name lifecycle or persistence. | `internal/naming`, standard library |
| `internal/namestore` | `package namestore` | Own threshold-authenticated current Namespace materializations, compact Record membership proofs, and the bounded compatible durable Namespace root with restart, tamper, stale-epoch, and atomic-publication rejection. | `internal/nameauthority`, `internal/namelease`, `internal/naming`, standard library |
| `internal/nameresolution` | `package nameresolution` | Own authenticated role selection, admitted immutable signed Record chains loaded from current durable naming state, fixed-size OHTTP Client/Relay/Gateway resolution and control exchange, opaque Gateway state binding, finite replay rejection, observer-safe counters, and fail-closed local behavior without a plaintext fallback. | `internal/nameadmission`, `internal/nameauthority` in behavior tests, `internal/namelease`, `internal/namestore`, `internal/naming`, `internal/network/state`, reviewed OHTTP/CIRCL closure, standard library |
| `internal/network/source` | `package source` | Own the finite Direct-Origin Source plan, credential binding, private TLS transport, material selector, ordering, and exposure identity. | standard library |
| `internal/network/state` | `package state` | Own authenticated Network State acceptance, Epoch/View verification, commitments, deterministic role-domain assignment, materializations, current/pending decisions, finite acquisition, clock confidence, Direct Source duty retention, its exclusive durable root, bounded immutable generations, distribution journal, canonical byte cursor, and atomic publication. Product tests own independent canonical vector builders. | `internal/network/duty`, `internal/network/source`, `internal/resource`, standard library |
| `tests/epochfixture/assignment` | `package assignment` | Independently construct deterministic role-domain assignments for black-box Network State fixtures; never serve maintained product code. | standard library |
| `tests/epochfixture/merkle` | `package merkle` | Independently construct canonical Epoch commitment roots and proofs for black-box Network State fixtures; never serve maintained product code. | standard library |
| `internal/route` | `package route` | Select one complete four-position Route from authenticated Network State, retain its Endpoint-local duties, perform bounded mutually authenticated sealed Introduction setup, and carry one or a finite declared capacity of authenticated Route Attachments through pressure-aware role-local actors with separate setup and active lifetimes, without receiving Service Instance material. | `internal/network/duty`, `internal/network/state`, `internal/resource`, standard library |
| `internal/routeplan` | `package routeplan` | Strictly load, validate, construct, and execute one bounded role-local sequence of Route actors without widening role knowledge. | `internal/network/state`, `internal/planfile`, `internal/route`, standard library |
| `internal/serviceconn` | `package serviceconn` | Own bounded local admission, current Service publication, exact Target/Instance authentication, opaque destination continuity, endpoint TLS, immutable recovery binding, volatile continuity, ordered/acknowledged Application bytes, finite replay state, Route Attachment cutover, and one honest terminal Service Connection result. | `internal/namelease` in behavior tests, standard library |
| `internal/serviceendpoint` | `package serviceendpoint` | Own role-local Endpoint process composition, strict bounded plan loading, scoped Application/administration IPC, reusable Route Attachment acceptance, and resource-observed cleanup behind `Run`. | `internal/applicationipc`, `internal/planfile`, `internal/serviceconn`, standard library |
| `internal/streamworkload` | `package streamworkload` | Generate, stream, pace, and validate deterministic bounded opaque workloads for external Application tracers, including the paired direct carrier baseline, without payload-sized allocation. | standard library |
| `internal/node` | `package node` | Own one bounded local Node identity credential, assignment admission, readiness, finite capacity, pressure reaction, durable Endpoint-local duty, drain, withdrawal, and terminal cleanup from narrow authenticated facts. | `internal/network/duty`, `internal/node/probe`, `internal/planfile`, `internal/resource`, standard library |
| `internal/node/probe` | `package probe` | Own the authenticated bounded role-probe listener, TLS trust, wire framing, replay rejection, admission pressure, and connection cleanup. | standard library |
| `internal/planfile` | `package planfile` | Own bounded operator-plan and credential reads, strict JSON decoding, exact hexadecimal fields, and TLS key-pair loading. | standard library |
| `internal/resource` | `package resource` | Own Linux cgroup-v2/rlimit and Go-runtime measurement, fixed H3 placement, hysteresis, and the finite NORMAL/PROTECT/DRAIN pressure decision behind `Check` and `Observe`; unsupported platforms explicitly refuse so consumers own readiness, admission, drain, and shutdown reactions without inventing resource policy. | standard library |
| `internal/architecture` | `package architecture` | Test repository structure, names, dependencies, formatting, and quality wiring. | standard library |
| `internal/applicationipc` | `package applicationipc` | Preserve the raw local Application byte stream and bind its terminal Result to either the compatible stream tail or an optional derived owner-only local control channel. | standard library |
| `internal/release` | `package release` | Own exclusive release-floor state and authenticate the accepted TUF-compatible H3 metadata profile, exact target identity, local binding, consecutive root rotation, build/protocol states, emergency transition, and durable non-decreasing floors. It returns a bounded Decision view and opaque authorization only for a verified accepted decision; it never downloads, installs, signs, exposes floor storage, repository administration, delegated targets, multi-repository maps, or ambient network/cache paths. | `github.com/theupdateframework/go-tuf/v2/metadata`, `github.com/theupdateframework/go-tuf/v2/metadata/trustedmetadata`, `golang.org/x/sys/windows` on Windows, standard library |
| `internal/updatetransaction` | `package updatetransaction` | Own one bounded compatible offline update transaction from a Release-issued opaque authorization through immutable staging, rollback reservation, stopped-runtime Adapter calls, atomic activation, self-test, committed journal evidence, idempotent replay, terminal inspection, and a caller-owned schema copy-on-write boundary without reading secrets or mutating Authority. | `internal/release`, `golang.org/x/sys/windows` on Windows, standard library |
## Carrier Lab command registry

| Command | Owning Module | Fixed responsibility |
|---|---|---|
| `bootstrap` | `internal/lab/preflight` | Orchestrate pinned host/container setup while reserving canonical evidence and verdicts for the pinned verifier. |
| `evaluate` | `internal/lab/preflight` | Verify prepared pinned inputs and write intermediate preflight evidence. |
| `finalize-cleanup` | `internal/lab/preflight` | Verify owned-resource removal and publish the final preflight verdict. |
| `compose-smoke` | `internal/lab/carrier` | Run the fixed two-role isolated scenario and own its lifecycle and summary. |
| `smoke-role` | `internal/lab/carrier` | Run one data-only role whose config names only its allowed peer. |
| `tooling-verify` | `internal/lab/tooling` | Verify the exact external `.deb` set against the committed tool lock. |
| `tooling-smoke` | `internal/lab/tooling` | Bind the runnable image to base/lock/source/binary identity, then own real shaping/capture, exact topology evidence, fail-closed verdict, and cleanup. |
| `tooling-role` | `internal/lab/tooling` | Run one fixed synthetic tracer, shaper, or capture role. |
| `direct-control` | `internal/lab/directcontrol` | Generate one ephemeral Target/Instance fixture and run the positive and fixed Direct TLS negative cases. |
| `direct-role` | `internal/lab/directcontrol` | Run one User or Service tracer role for the fixed Direct TLS control. |
| `direct-tamper` | `internal/lab/directcontrol` | Modify one TLS-protected record without receiving endpoint or Application knowledge. |
| `native-run` | `internal/lab/nativecircuit` | Run the fixed isolated native C-5/C2 development smoke and retain its bounded verdict. |
| `native-role` | `internal/lab/nativecircuit` | Run one role-local native User, Service, relay, Rendezvous, or Introduction process. |
| `native-tool-role` | `internal/lab/tooling` | Apply real link shaping or capture for one native role namespace with one exact capability. |
| `native-negative` | `internal/lab/nativecircuit` | Execute one fixed fail-closed R-013 negative inside the immutable application image. |
| `route-experiment` | `internal/lab/routecomparison` | Run the frozen Direct/C-3/C-5 comparison, required negatives, conditional Tor reference, and canonical verdict without building or downloading. |
| `pressure-memory` | `internal/lab/tooling` | Hold one bounded resident allocation, optionally alongside an incomplete local TCP setup, to exercise real container pressure and admission. |

The architecture gate parses this table and rejects an unregistered or stale
package, a mismatched Go declaration, or a current project import absent from
the owning row's `May import` column.
