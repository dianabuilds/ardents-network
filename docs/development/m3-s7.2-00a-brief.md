# M3-autonomous brief — S7.2-00a authenticated-time handoff

Status: **accepted launch brief; implementation authorized only from the exact
contract commit SHA supplied by Codex.**

## Role and authority

Act only as the bounded `M3-autonomous` implementer. Do not redesign, review,
or accept the contract. The authorities are, in order:

1. accepted ADRs and
   [`stage-7-lifecycle-spec.md`](stage-7-lifecycle-spec.md);
2. accepted S7.2-00a in
   [`stage-7-s7.2-task-contracts.md`](stage-7-s7.2-task-contracts.md);
3. the accepted
   [`stage-7-s7.2-transaction-format-proposal.md`](stage-7-s7.2-transaction-format-proposal.md);
4. this brief.

If the proposal is not accepted or the supplied baseline does not match HEAD,
return `contract-blocked` without an edit. Do not copy code from either
abandoned S7.2-01 attempt.

## Exact task

Extend the existing `releasedecision.Decision` handoff with four time facts
that the Module already validates:

```go
ReferenceTime               time.Time
BuildSafetyNoNewWorkAfter   time.Time
BuildSafetyTerminateAfter   time.Time
ProtocolTransitionDeadline time.Time
```

Populate them only after target descriptor decoding, validation, artifact and
local-identity matching have succeeded:

- `ReferenceTime` is the exact fixed `LocalEnvironment.RefTime`, normalized to
  UTC by the existing Evaluate flow;
- the two Build Safety fields are the exact authenticated descriptor values;
- `ProtocolTransitionDeadline` is the authenticated emergency expiry when the
  accepted/rejected exact target identity carries an emergency transition and
  zero otherwise.

An identity-invalid path that never returns the existing exact target identity
must keep all four fields zero. A later lifecycle outcome may retain them only
where `Decision` already intentionally returns that authenticated target
identity. Do not change outcome selection, floor publication, notices, command
JSON, or metadata parsing.

## Allowed files and caps

Only these files may change:

- `internal/releasedecision/inputs.go`;
- `internal/releasedecision/target_identity.go`;
- `internal/releasedecision/public_vector_test.go`;
- `internal/releasedecision/cells_test.go`.

Caps are conjunctive:

- at most `120` added or changed production LOC;
- no new production/test file;
- every production file remains at most `250` physical lines and every test
  file at most `500`;
- no new exported declaration, dependency, command field, clock Interface,
  parser, package, or documentation edit.

If the task cannot fit, report `scope-blocked` without a commit.

## Frozen tests

1. Extend the existing public-vector test to require:
   - `ReferenceTime == 2030-01-02T03:04:05Z`;
   - `BuildSafetyNoNewWorkAfter == 2030-02-01T03:04:05Z`;
   - `BuildSafetyTerminateAfter == 2030-07-01T03:04:05Z`;
   - zero `ProtocolTransitionDeadline`.
2. Extend `TestEvaluateB14ValidEmergencyTransition` to require the exact
   configured emergency expiry in `ProtocolTransitionDeadline` and the same
   fixed reference/build bounds used by its synthetic repository.
3. In an existing identity-invalid test whose descriptor or local identity
   fails before an exact target Decision is returned, require all four fields
   to be zero.
4. Existing public-vector expected JSON and `cmd/ardents-release offline-import`
   bytes remain unchanged. Do not add these Go fields to rendered JSON.

Expected values must come from the saved vector or explicit test constants,
not from production helpers or production descriptor fields.

## Required checks

Run all of these from the unchanged source tree:

```text
gofmt -w <only changed Go files>
go test ./internal/releasedecision -count=1
go test -race ./internal/releasedecision -count=1
go test ./cmd/ardents-release -count=1
GOOS=linux GOARCH=amd64 go build ./internal/releasedecision ./cmd/ardents-release
GOOS=windows GOARCH=amd64 go build ./internal/releasedecision ./cmd/ardents-release
make quick-check
make check
```

Use PowerShell environment syntax on Windows rather than copying the Unix
assignment spelling literally. Build outputs must remain outside the
repository. A source edit after a platform build invalidates that evidence.

## Handoff and commit

Commit exactly once only when every check is green. The handoff must contain:

- baseline and final commit SHA;
- exact files and production LOC delta;
- acceptance-criterion-to-test mapping;
- exact command results, including any skipped command as a failure;
- confirmation that command JSON did not change;
- `git status --short` after commit.

End the handoff with exactly:

`implemented, not accepted`
