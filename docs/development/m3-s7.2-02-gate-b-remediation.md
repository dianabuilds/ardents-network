# S7.2-02 Gate B controller remediation

Status: **controller-approved; executable from the accepted policy commit**

Baseline: `f4acab3454acf087dd26795510978a6e9d1a969e`

Authority remains the accepted S7.2-02 v2 contract and frozen Gate A oracle.
This note records the controller review of the interrupted first Gate B pass. It
does not change any expected Result, fixture byte, journal byte, cleanup order,
or public Interface.

The Product Owner accepted the S7.2 production-file size amendment on
2026-08-21. Start from the controller commit that carries the synchronized
authoritative rules; the existing temporary worktree is evidence to salvage,
not the new baseline.

## Frozen evidence

The eight Gate A test files are controller-frozen. M3 may bind existing private
fault seams or add tests for a newly discovered defect, but may not change a
frozen expected value, physical fixture, record encoding, row classification,
or before/after tree commitment.

Before resuming, recompute the eight recorded SHA-256 values and stop with
`scope-blocked` if any differs from the controller snapshot.

| Frozen Gate A file | SHA-256 |
|---|---|
| `recovery_checkpoint_rows_test.go` | `3c80506eef767a59ecfeea1582dd95e84353ec3b49d3fff1d92eb885da88c7b7` |
| `recovery_checkpoint_test.go` | `96a874c314aa5c850142d6eac64e96c26f38904ec6ac71ebc26921aed473176a` |
| `recovery_cleanup_lock_test.go` | `d40abbebc7ba9df80ecaeef3428dc00dae1e28419e65799894fe80950da1a47c` |
| `recovery_cleanup_plan_test.go` | `9632df2212072653f1b1e6b8116d6c879d8c0a2de2fc071ef4a6301e73113a6d` |
| `recovery_corruption_field_test.go` | `10f60af65aa11e9378e325112f42208290431c129c9e8bb2b78e387a16de3d53` |
| `recovery_corruption_test.go` | `45273241a212aaf88cb14523da9b3f4fceefa2669228e9ee1c38ea563e523715` |
| `recovery_fixture_test.go` | `96d5fea1ec012762604795533b14726dc340a5317b86d0f56d128cc64bf54db6` |
| `recovery_inventory_assertion_test.go` | `68f1a8426216c440df167d12c46eda8feaecd277f049c93b4f14877d9a6ad78a` |

The first Gate B pass created only:

- `lock_nonwindows.go`;
- `lock_windows.go`; and
- `recovery_inventory.go`.

There is no production commit to preserve.

## Disposition of the partial implementation

### Platform lock files: retain the responsibility, rewrite the evidence flow

The build tags, private names, and one-file-per-platform responsibility may be
retained. The current implementation is not acceptable.

Both platforms must:

1. open the installer-created lock without create, replacement, unlink, or
   retry;
2. acquire the non-blocking OS lock before treating any identity observation as
   authoritative;
3. prove through the held handle that the object is a direct, regular, empty,
   single-link file and that the current path names that same object;
4. classify only the platform's actual lock-contention result as `busy`;
5. classify permission, unsupported-operation, identity, and other syscall
   failures as invalid lock evidence; and
6. observe and join every unlock and close error, including failed-acquire
   cleanup.

On non-Windows, `file.Name()` is not identity evidence. Compare held-handle
`fstat` with non-following path evidence after `Flock`, including device, inode,
type, and link count. Only `EWOULDBLOCK`/`EAGAIN` proves contention.

On Windows, `GetFileAttributes(path)` plus `file.Name()` is not identity
evidence. Use handle information and a non-reparse path handle to prove the
same volume/file identity and `NumberOfLinks == 1`. Only
`ERROR_LOCK_VIOLATION` proves contention; `ERROR_ACCESS_DENIED` does not.

### `recovery_inventory.go`: retain only the named responsibility

Discard the current fact model and implementation. Local patching is rejected:
it cannot supply enough immutable physical evidence for a pure planner.

The replacement performs one bounded read-only pass after lock ownership and
records, without classifying a recovery row:

- exact root children and their direct physical kind;
- exact marker and current raw bytes plus identity facts;
- every canonical generation directory and exact artifact/manifest raw facts;
- every staging child and exact candidate facts;
- every transaction directory, including its canonical name and physical
  children;
- every journal directory child as a raw named entry, including gaps, extras,
  aliases, and non-regular objects; and
- every candidate current-temp name, identity, and bounded raw bytes.

The interrupted transaction is discovered by enumerating `transactions/`; it
is not inferred from `current.Transaction`. The inventory must expose enough
facts for the planner to reject a second transaction, reused generation,
missing journal directory, noncanonical numeric name, unknown child, malformed
marker, and link/reparse evidence without another filesystem read.

Inventory does not decode journal entries, verify their chain, classify
R00-R14, construct a Result, or execute cleanup. Those responsibilities remain
in `recovery_journal.go`, `recovery_plan.go`, `recovery.go`, and `cleanup.go`.

Every file read is size-bounded before allocation and revalidates the opened
handle. `os.ReadFile` is forbidden in the recovery inventory. Numeric names are
canonical only when parsing and formatting round-trip byte-for-byte. A current
temporary name is exactly `.current.` plus 16 lowercase hexadecimal characters
plus `.tmp`.

## Required implementation order

1. Recompute and report the frozen Gate A hashes and prove the production diff
   still contains only the three untracked files above.
2. Repair both lock implementations and run the focused lock tests plus both
   cross-builds.
3. Replace the inventory implementation and run only inventory/invalid-corpus
   tests. Do not add classification to make a row green.
4. Implement the private journal validator over inventory-provided raw facts.
5. Implement the pure R00-R14 planner and show its deterministic typed operation
   plans in focused tests.
6. Implement the cleanup executor with the accepted pre/post observation rule
   and per-invocation fault seam.
7. Bind `Recover` and the existing Apply interruption points without changing
   exported declarations or caller production.
8. Run the entire Gate B command list and report exact output, file counts,
   net-new/total Module LOC, and any file above the accepted review threshold.
9. Stop for controller review. Do not commit until the controller independently
   verifies the worktree and explicitly authorizes the single Gate B commit.

## Size-policy handling

Correctness is not traded for a line target. After the proposed amendment is
accepted, 250 lines is a review trigger rather than a build failure; the
interim 500-line all-Go ceiling and accepted task-wide caps remain hard.

For each production file over 250 lines, the Gate B report must state its one
responsibility, the invariants kept local, the rejected obvious split, and the
focused behavior tests. This permission does not merge inventory, journal,
planning, cleanup, or platform responsibilities back together.

## Stop conditions

Return `scope-blocked` without commit if any of the following occurs:

- a frozen Gate A hash or expectation changed;
- the accepted record format or public Interface would need to change;
- a platform cannot prove lock handle/path identity with the accepted
  dependencies;
- a task-wide or interim 500-line hard cap would be exceeded;
- a required R00-R14, invalid-corpus, cleanup, race, or cross-build check cannot
  pass without weakening its oracle; or
- the worktree contains unrelated changes.
