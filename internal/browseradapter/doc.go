// Package browseradapter owns the optional Browser product's loopback HTTP
// presentation, Browser Entry state, and exact-name demand lifecycle. It uses
// only the local Endpoint Connection Interface and owns no Network State,
// Entry, Target, Route, Service Administration, or custody authority.
package browseradapter
