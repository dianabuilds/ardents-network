// Package inspection composes the bounded H4-6A disclosure reader with the
// independently owned Enrollment, Release, and Network State verifiers. It
// owns only inspection-local state roots and never starts an Endpoint.
package inspection
