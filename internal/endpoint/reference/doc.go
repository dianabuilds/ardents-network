// Package reference owns one scoped loopback presentation origin for an
// already-authenticated Service Target and a bounded static Reference Site.
// It neither parses Target Links nor selects a Target, connects to a Service,
// proxies ordinary URLs, or changes browser or system configuration.
package reference
