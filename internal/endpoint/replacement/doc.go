// Package replacement owns the H4-1B Ubuntu Endpoint-program binding between
// one Release-authorized artifact and the exact executable allowed to start.
// It owns no artifact source, network work, Authority material, or service
// manager; a foreground caller supplies those explicit operations.
package replacement
