// Package provision initializes protected Node state, configuration, identity,
// and first-Operator enrollment material. It does not own normal runtime
// lifecycle or recurring credential administration.
package provision
