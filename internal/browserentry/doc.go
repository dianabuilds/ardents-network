// Package browserentry owns the per-user Firefox native-host handoff for one
// Endpoint-created alpha loopback proxy. It persists no Service Name, Target,
// or application data and never resolves, dials, or proxies a destination.
package browserentry
