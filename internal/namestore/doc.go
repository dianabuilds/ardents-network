// Package namestore persists threshold-authenticated current Namespace
// materializations and produces compact Record membership proofs through the
// existing atomic Network State store without exposing its filesystem protocol.
package namestore
