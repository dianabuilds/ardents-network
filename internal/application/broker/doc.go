// Package broker owns volatile Local Grant admission and active Connection
// lifecycle for generic, explicitly unqualified local Application attachments.
// One-use capability expiry is separate from active lease termination, and
// Connection and Administration have finite independent budgets.
package broker
