// Package targetlink owns the closed canonical Target Link v1 text grammar.
//
// It validates only an explicit network-bound Service Target. It neither
// resolves reachability nor authenticates a remote Service Instance.
package targetlink
