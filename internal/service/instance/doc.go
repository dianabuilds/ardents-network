// Package instance owns one host-local Service Instance root and its public
// credential request. Private Instance and Introduction keys never leave the
// package through its public interface.
package instance
