// Package naming owns canonical Service Name parsing, normalization, and encoding rules for Stage 6.
//
// It does not perform persistence or protocol encoding on its own; those details
// belong to the concrete lifecycle and resolution slices.
package naming
